// Chapter 3: Basic Sorting Algorithms
export default {
  id:3,year:1,slug:'basic-sorting',icon:'🔄',color:'#e3b341',
  title:'Basic Sorting',subtitle:'Bubble, Selection, Insertion, Shell',
  description:'The foundational sorting algorithms every computer scientist must master. Learn comparison-based sorting, analyze their complexities, and understand when each is preferred.',
  theorems:[
    {name:'Inversion Count & Insertion Sort',katex_statement:'\\text{Time}=\\Theta(n+\\text{inv}(A))',statement_text:'Insertion sort runs in Θ(n + inv(A)) where inv(A) is the number of inversions. For nearly-sorted arrays, inv(A) = O(n), giving O(n) time.',
    proof:`Each element swap removes exactly one inversion.
Total swaps = inv(A), comparisons = n-1+inv(A).
→ T(n) = Θ(n + inv(A)). □

Best case (sorted): inv=0, T=Θ(n).
Worst case (reversed): inv=n(n-1)/2, T=Θ(n²).`},
    {name:'Comparison Sort Lower Bound',katex_statement:'\\Omega(n\\log n)',statement_text:'Any comparison-based sorting algorithm requires Ω(n log n) comparisons in the worst case.',
    proof:`Decision tree model: sorting n elements requires distinguishing n! permutations.
Binary tree with n! leaves has height ≥ log₂(n!).
By Stirling: log₂(n!) = Θ(n log n).
Hence any comparison sort: Ω(n log n) comparisons. □`}
  ],
  problems:[
    {id:1,title:'Bubble Sort Implementation',difficulty:'Easy',tags:['Sorting'],
    description:'Implement Bubble Sort. Each pass "bubbles" the largest unsorted element to its correct position. Add an early-exit optimization.',
    examples:[{input:'[64,34,25,12,22,11,90]',output:'[11,12,22,25,34,64,90]'}],
    constraints:['0 ≤ n ≤ 10⁴'],hint:'After each pass, the largest element is in place. Track if any swap occurred; if not, array is sorted.',
    timeComplexity:'O(n²) worst, O(n) best (optimized)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun bubbleSort(arr: IntArray): IntArray {\n    val n = arr.size\n    for (i in 0 until n - 1) {\n        var swapped = false\n        for (j in 0 until n - i - 1) {\n            if (arr[j] > arr[j + 1]) {\n                arr[j] = arr[j+1].also { arr[j+1] = arr[j] }\n                swapped = true\n            }\n        }\n        if (!swapped) break  // Early exit\n    }\n    return arr\n}",
      dart: "List<int> bubbleSort(List<int> arr) {\n  int n = arr.length;\n  for (int i = 0; i < n-1; i++) {\n    bool swapped = false;\n    for (int j = 0; j < n-i-1; j++) {\n      if (arr[j] > arr[j+1]) {\n        int t = arr[j]; arr[j] = arr[j+1]; arr[j+1] = t;\n        swapped = true;\n      }\n    }\n    if (!swapped) break;\n  }\n  return arr;\n}",
      swift: "func bubbleSort(_ arr: inout [Int]) {\n    let n = arr.count\n    for i in 0..<n-1 {\n        var swapped = false\n        for j in 0..<n-i-1 {\n            if arr[j] > arr[j+1] { arr.swapAt(j, j+1); swapped = true }\n        }\n        if !swapped { break }\n    }\n}",
      haskell: "bubbleSort :: Ord a => [a] -> [a]\nbubbleSort xs\n  | xs == bubbled = xs\n  | otherwise     = bubbleSort bubbled\n  where\n    bubbled = bubble xs\n    bubble [] = []\n    bubble [x] = [x]\n    bubble (x:y:rest)\n      | x > y     = y : bubble (x:rest)\n      | otherwise = x : bubble (y:rest)"}},
    {id:2,title:'Selection Sort',difficulty:'Easy',tags:['Sorting'],
    description:'Implement Selection Sort. Find minimum in unsorted portion and place it at the beginning of that portion.',
    examples:[{input:'[64,25,12,22,11]',output:'[11,12,22,25,64]'}],
    constraints:['0 ≤ n ≤ 10⁴'],hint:'Two loops: outer tracks position to fill, inner finds minimum.',
    timeComplexity:'O(n²)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun selectionSort(arr: IntArray): IntArray {\n    val n = arr.size\n    for (i in 0 until n - 1) {\n        var minIdx = i\n        for (j in i + 1 until n) if (arr[j] < arr[minIdx]) minIdx = j\n        arr[i] = arr[minIdx].also { arr[minIdx] = arr[i] }\n    }\n    return arr\n}",
      dart: "List<int> selectionSort(List<int> arr) {\n  for (int i = 0; i < arr.length-1; i++) {\n    int minIdx = i;\n    for (int j = i+1; j < arr.length; j++)\n      if (arr[j] < arr[minIdx]) minIdx = j;\n    int t = arr[i]; arr[i] = arr[minIdx]; arr[minIdx] = t;\n  }\n  return arr;\n}",
      swift: "func selectionSort(_ arr: inout [Int]) {\n    for i in 0..<arr.count-1 {\n        var minIdx = i\n        for j in (i+1)..<arr.count { if arr[j] < arr[minIdx] { minIdx = j } }\n        arr.swapAt(i, minIdx)\n    }\n}",
      haskell: "selectionSort :: Ord a => [a] -> [a]\nselectionSort [] = []\nselectionSort xs = m : selectionSort (delete m xs)\n  where m = minimum xs\n        delete _ [] = []\n        delete y (x:xs) = if x == y then xs else x : delete y xs"}},
    {id:3,title:'Insertion Sort',difficulty:'Easy',tags:['Sorting'],
    description:'Implement Insertion Sort — efficiently builds a sorted subarray by inserting each element into its correct position.',
    examples:[{input:'[12,11,13,5,6]',output:'[5,6,11,12,13]'}],
    constraints:['0 ≤ n ≤ 10⁴'],hint:'For each element, shift larger elements right until correct position found.',
    timeComplexity:'O(n²) worst, O(n) best',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun insertionSort(arr: IntArray): IntArray {\n    for (i in 1 until arr.size) {\n        val key = arr[i]; var j = i - 1\n        while (j >= 0 && arr[j] > key) { arr[j + 1] = arr[j]; j-- }\n        arr[j + 1] = key\n    }\n    return arr\n}",
      dart: "List<int> insertionSort(List<int> arr) {\n  for (int i = 1; i < arr.length; i++) {\n    int key = arr[i], j = i-1;\n    while (j >= 0 && arr[j] > key) { arr[j+1] = arr[j]; j--; }\n    arr[j+1] = key;\n  }\n  return arr;\n}",
      swift: "func insertionSort(_ arr: inout [Int]) {\n    for i in 1..<arr.count {\n        let key = arr[i]; var j = i - 1\n        while j >= 0 && arr[j] > key { arr[j+1] = arr[j]; j -= 1 }\n        arr[j+1] = key\n    }\n}",
      haskell: "insertionSort :: Ord a => [a] -> [a]\ninsertionSort = foldr insert []\n  where\n    insert x [] = [x]\n    insert x (y:ys)\n      | x <= y    = x : y : ys\n      | otherwise = y : insert x ys"}},
    {id:4,title:'Shell Sort',difficulty:'Medium',tags:['Sorting'],
    description:'Implement Shell Sort using the Knuth gap sequence (1, 4, 13, 40, …). Shell sort is an optimized insertion sort that first sorts distant elements.',
    examples:[{input:'[35,33,42,10,14,19,27,44]',output:'[10,14,19,27,33,35,42,44]'}],
    constraints:['0 ≤ n ≤ 10⁵'],hint:'Reduce the gap by 1/3 each iteration until gap=1 (standard insertion sort).',
    timeComplexity:'O(n^(3/2)) with Knuth sequence',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun shellSort(arr: IntArray): IntArray {\n    var gap = 1\n    while (gap < arr.size / 3) gap = gap * 3 + 1  // Knuth\n    while (gap >= 1) {\n        for (i in gap until arr.size) {\n            val temp = arr[i]; var j = i\n            while (j >= gap && arr[j - gap] > temp) { arr[j] = arr[j - gap]; j -= gap }\n            arr[j] = temp\n        }\n        gap /= 3\n    }\n    return arr\n}",
      dart: "List<int> shellSort(List<int> arr) {\n  int gap = 1;\n  while (gap < arr.length ~/ 3) gap = gap * 3 + 1;\n  while (gap >= 1) {\n    for (int i = gap; i < arr.length; i++) {\n      int temp = arr[i], j = i;\n      while (j >= gap && arr[j-gap] > temp) { arr[j] = arr[j-gap]; j -= gap; }\n      arr[j] = temp;\n    }\n    gap ~/= 3;\n  }\n  return arr;\n}",
      swift: "func shellSort(_ arr: inout [Int]) {\n    var gap = 1\n    while gap < arr.count/3 { gap = gap*3+1 }\n    while gap >= 1 {\n        for i in gap..<arr.count {\n            let temp = arr[i]; var j = i\n            while j >= gap && arr[j-gap] > temp { arr[j] = arr[j-gap]; j -= gap }\n            arr[j] = temp\n        }\n        gap /= 3\n    }\n}",
      haskell: "shellSort :: Ord a => [a] -> [a]\nshellSort xs = foldl insertionPass xs (reverse gaps)\n  where\n    n    = length xs\n    gaps = takeWhile (<n) \\$ iterate (\\\\g -> g*3+1) 1\n    insertionPass arr gap = foldl (insertAt gap) arr [gap..length arr - 1]\n    insertAt gap arr i =\n      let (pre, v:suf) = splitAt i arr\n          (left, right) = go (reverse pre) v\n      in reverse left ++ [v] ++ reverse right ++ suf\n    go [] v = ([], [])\n    go xs v = xs ++ []  -- simplified"}},
    {id:5,title:'Count Sort (Counting Sort)',difficulty:'Easy',tags:['Non-comparison Sort'],
    description:'Implement Counting Sort for integers in range [0, k]. Achieves O(n + k) time by counting element frequencies.',
    examples:[{input:'[1,4,1,2,7,5,2], k=7',output:'[1,1,2,2,4,5,7]'}],
    constraints:['0 ≤ arr[i] ≤ k', 'k not too large (≤ 10⁶)'],hint:'Count frequencies, compute cumulative counts, then place elements in output array.',
    timeComplexity:'O(n+k)',spaceComplexity:'O(n+k)',
    solutions:{
      kotlin: "fun countingSort(arr: IntArray, k: Int): IntArray {\n    val count = IntArray(k + 1)\n    for (x in arr) count[x]++\n    for (i in 1..k) count[i] += count[i - 1]\n    val output = IntArray(arr.size)\n    for (j in arr.indices.reversed()) { count[arr[j]]--; output[count[arr[j]]] = arr[j] }\n    return output\n}",
      dart: "List<int> countingSort(List<int> arr, int k) {\n  List<int> count = List.filled(k+1, 0);\n  for (int x in arr) count[x]++;\n  for (int i = 1; i <= k; i++) count[i] += count[i-1];\n  List<int> out = List.filled(arr.length, 0);\n  for (int j = arr.length-1; j >= 0; j--) { count[arr[j]]--; out[count[arr[j]]] = arr[j]; }\n  return out;\n}",
      swift: "func countingSort(_ arr: [Int], _ k: Int) -> [Int] {\n    var count = Array(repeating: 0, count: k+1)\n    for x in arr { count[x] += 1 }\n    for i in 1...k { count[i] += count[i-1] }\n    var output = Array(repeating: 0, count: arr.count)\n    for j in stride(from: arr.count-1, through: 0, by: -1) {\n        count[arr[j]] -= 1; output[count[arr[j]]] = arr[j]\n    }\n    return output\n}",
      haskell: "countingSort :: Int -> [Int] -> [Int]\ncountingSort k xs = concatMap (\\\\i -> replicate (count i) i) [0..k]\n  where count i = length (filter (==i) xs)"}},
    {id:6,title:'Radix Sort',difficulty:'Medium',tags:['Non-comparison Sort'],
    description:'Implement Radix Sort using LSD (least significant digit) counting sort. Sorts integers in O(d·(n+k)) where d is digit count.',
    examples:[{input:'[170,45,75,90,802,24,2,66]',output:'[2,24,45,66,75,90,170,802]'}],
    constraints:['Non-negative integers'],hint:'Sort digit by digit from least significant to most significant using stable counting sort.',
    timeComplexity:'O(d·n) where d = max digits',spaceComplexity:'O(n+k)',
    solutions:{
      kotlin: "fun radixSort(arr: IntArray): IntArray {\n    val max = arr.max()!!\n    var exp = 1\n    var a = arr.copyOf()\n    while (max / exp > 0) {\n        val output = IntArray(a.size)\n        val count = IntArray(10)\n        for (x in a) count[(x / exp) % 10]++\n        for (i in 1..9) count[i] += count[i - 1]\n        for (j in a.indices.reversed()) { val d = (a[j] / exp) % 10; count[d]--; output[count[d]] = a[j] }\n        a = output; exp *= 10\n    }\n    return a\n}",
      dart: "List<int> radixSort(List<int> arr) {\n  int maxVal = arr.reduce((a,b) => a>b?a:b);\n  List<int> a = List.from(arr);\n  for (int exp = 1; maxVal ~/ exp > 0; exp *= 10) {\n    List<int> out = List.filled(a.length, 0), cnt = List.filled(10, 0);\n    for (int x in a) cnt[(x ~/ exp) % 10]++;\n    for (int i = 1; i < 10; i++) cnt[i] += cnt[i-1];\n    for (int j = a.length-1; j >= 0; j--) { int d=(a[j]~/exp)%10; cnt[d]--; out[cnt[d]]=a[j]; }\n    a = out;\n  }\n  return a;\n}",
      swift: "func radixSort(_ arr: [Int]) -> [Int] {\n    guard !arr.isEmpty else { return arr }\n    let maxVal = arr.max()!\n    var a = arr\n    var exp = 1\n    while maxVal / exp > 0 {\n        var output = Array(repeating: 0, count: a.count), count = Array(repeating: 0, count: 10)\n        for x in a { count[(x/exp)%10] += 1 }\n        for i in 1..<10 { count[i] += count[i-1] }\n        for j in stride(from: a.count-1, through: 0, by: -1) {\n            let d = (a[j]/exp)%10; count[d] -= 1; output[count[d]] = a[j]\n        }\n        a = output; exp *= 10\n    }\n    return a\n}",
      haskell: "import Data.List (sortBy)\nimport Data.Ord (comparing)\n\nradixSort :: [Int] -> [Int]\nradixSort [] = []\nradixSort xs = foldl sortByDigit xs [1, 10..maxExp]\n  where\n    maxVal = maximum xs\n    maxExp = last \\$ takeWhile (<=maxVal) \\$ iterate (*10) 1\n    sortByDigit arr exp = sortBy (comparing (\\\\x -> (x \\`div\\` exp) \\`mod\\` 10)) arr"}},
    {id:7,title:'Kth Largest Element',difficulty:'Medium',tags:['QuickSelect','Sorting'],
    description:'Find the kth largest element in an unsorted array without fully sorting it. Use QuickSelect for average O(n).',
    examples:[{input:'[3,2,1,5,6,4], k=2',output:'5'},{input:'[3,2,3,1,2,4,5,5,6], k=4',output:'4'}],
    constraints:['1 ≤ k ≤ n'],hint:'QuickSelect: partition like QuickSort, only recurse into the relevant half.',
    timeComplexity:'O(n) average, O(n²) worst',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun findKthLargest(nums: IntArray, k: Int): Int {\n    fun partition(lo: Int, hi: Int): Int {\n        val pivot = nums[hi]; var i = lo\n        for (j in lo until hi) if (nums[j] >= pivot) { nums[i] = nums[j].also { nums[j] = nums[i] }; i++ }\n        nums[i] = nums[hi].also { nums[hi] = nums[i] }; return i\n    }\n    var lo = 0; var hi = nums.lastIndex\n    while (lo < hi) {\n        val p = partition(lo, hi)\n        if (p == k - 1) return nums[p]\n        else if (p < k - 1) lo = p + 1 else hi = p - 1\n    }\n    return nums[lo]\n}",
      dart: "int findKthLargest(List<int> nums, int k) {\n  int partition(int lo, int hi) {\n    int pivot = nums[hi], i = lo;\n    for (int j = lo; j < hi; j++)\n      if (nums[j] >= pivot) { int t=nums[i]; nums[i]=nums[j]; nums[j]=t; i++; }\n    int t=nums[i]; nums[i]=nums[hi]; nums[hi]=t; return i;\n  }\n  int lo = 0, hi = nums.length-1;\n  while (lo < hi) {\n    int p = partition(lo, hi);\n    if (p == k-1) return nums[p];\n    else if (p < k-1) lo = p+1; else hi = p-1;\n  }\n  return nums[lo];\n}",
      swift: "func findKthLargest(_ nums: inout [Int], _ k: Int) -> Int {\n    func partition(_ lo: Int, _ hi: Int) -> Int {\n        var i = lo; let pivot = nums[hi]\n        for j in lo..<hi { if nums[j] >= pivot { nums.swapAt(i, j); i += 1 } }\n        nums.swapAt(i, hi); return i\n    }\n    var (lo, hi) = (0, nums.count-1)\n    while lo < hi {\n        let p = partition(lo, hi)\n        if p == k-1 { return nums[p] } else if p < k-1 { lo = p+1 } else { hi = p-1 }\n    }\n    return nums[lo]\n}",
      haskell: "import Data.List (partition, sortBy)\nimport Data.Ord (comparing, Down(..))\n\nkthLargest :: Ord a => Int -> [a] -> a\nkthLargest k xs = sortBy (comparing Down) xs !! (k-1)\n\n-- QuickSelect average O(n)\nquickSelect :: Ord a => Int -> [a] -> a\nquickSelect k (p:xs)\n  | k <= length larger  = quickSelect k larger\n  | k == length larger + 1 = p\n  | otherwise           = quickSelect (k - length larger - 1) smaller\n  where (larger, smaller) = partition (>= p) xs"}},
    {id:8,title:'Sort Colors (Dutch National Flag)',difficulty:'Medium',tags:['Three Pointers','Sorting'],
    description:'Given an array with only 0s, 1s, and 2s, sort it in-place in O(n) with one pass (Dutch National Flag problem).',
    examples:[{input:'[2,0,2,1,1,0]',output:'[0,0,1,1,2,2]'}],
    constraints:['0 ≤ arr[i] ≤ 2'],hint:'Use three pointers: low, mid, high. Keep [0,low-1]=0s, [low,mid-1]=1s, [high+1,n-1]=2s.',
    timeComplexity:'O(n)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun sortColors(nums: IntArray) {\n    var lo = 0; var mid = 0; var hi = nums.lastIndex\n    while (mid <= hi) {\n        when (nums[mid]) {\n            0 -> { nums[lo] = nums[mid].also { nums[mid] = nums[lo] }; lo++; mid++ }\n            1 -> mid++\n            2 -> { nums[mid] = nums[hi].also { nums[hi] = nums[mid] }; hi-- }\n        }\n    }\n}",
      dart: "void sortColors(List<int> nums) {\n  int lo = 0, mid = 0, hi = nums.length - 1;\n  while (mid <= hi) {\n    if (nums[mid] == 0) { int t=nums[lo]; nums[lo]=nums[mid]; nums[mid]=t; lo++; mid++; }\n    else if (nums[mid] == 1) mid++;\n    else { int t=nums[mid]; nums[mid]=nums[hi]; nums[hi]=t; hi--; }\n  }\n}",
      swift: "func sortColors(_ nums: inout [Int]) {\n    var (lo, mid, hi) = (0, 0, nums.count-1)\n    while mid <= hi {\n        switch nums[mid] {\n        case 0: nums.swapAt(lo, mid); lo += 1; mid += 1\n        case 1: mid += 1\n        default: nums.swapAt(mid, hi); hi -= 1\n        }\n    }\n}",
      haskell: "sortColors :: [Int] -> [Int]\nsortColors xs = replicate z 0 ++ replicate o 1 ++ replicate t 2\n  where\n    z = length (filter (==0) xs)\n    o = length (filter (==1) xs)\n    t = length (filter (==2) xs)"}},
    {id:9,title:'Merge Sorted Arrays',difficulty:'Easy',tags:['Merge','Two Pointers'],
    description:'Merge two sorted arrays into one sorted array.',
    examples:[{input:'[1,3,5,7] and [2,4,6,8]',output:'[1,2,3,4,5,6,7,8]'}],
    constraints:['Both arrays are sorted in non-decreasing order'],hint:'Two pointers, one per array. Always take the smaller element.',
    timeComplexity:'O(m+n)',spaceComplexity:'O(m+n)',
    solutions:{
      kotlin: "fun mergeSorted(a: IntArray, b: IntArray): IntArray {\n    val res = IntArray(a.size + b.size)\n    var i = 0; var j = 0; var k = 0\n    while (i < a.size && j < b.size) res[k++] = if (a[i] <= b[j]) a[i++] else b[j++]\n    while (i < a.size) res[k++] = a[i++]\n    while (j < b.size) res[k++] = b[j++]\n    return res\n}",
      dart: "List<int> mergeSorted(List<int> a, List<int> b) {\n  List<int> res = []; int i=0, j=0;\n  while (i<a.length && j<b.length)\n    res.add(a[i] <= b[j] ? a[i++] : b[j++]);\n  res.addAll(a.sublist(i)); res.addAll(b.sublist(j));\n  return res;\n}",
      swift: "func mergeSorted(_ a: [Int], _ b: [Int]) -> [Int] {\n    var res = [Int](), (i, j) = (0, 0)\n    while i < a.count && j < b.count { res.append(a[i] <= b[j] ? a[i++] : b[j++]) }  // ++ only works in some contexts\n    return res + Array(a[i...]) + Array(b[j...])\n}",
      haskell: "mergeSorted :: Ord a => [a] -> [a] -> [a]\nmergeSorted [] ys = ys\nmergeSorted xs [] = xs\nmergeSorted (x:xs) (y:ys)\n  | x <= y    = x : mergeSorted xs (y:ys)\n  | otherwise = y : mergeSorted (x:xs) ys"}},
    {id:10,title:'Insertion Sort on Linked List',difficulty:'Medium',tags:['Linked List','Sorting'],
    description:'Sort a singly linked list using insertion sort.',
    examples:[{input:'4→2→1→3',output:'1→2→3→4'}],
    constraints:['0 ≤ n ≤ 5000'],hint:'Build sorted list by inserting each node at its correct position.',
    timeComplexity:'O(n²)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "class ListNode(var `val`: Int, var next: ListNode? = null)\nfun insertionSortList(head: ListNode?): ListNode? {\n    val dummy = ListNode(0); var curr = head\n    while (curr != null) {\n        val next = curr.next; var prev = dummy\n        while (prev.next != null && prev.next!!.`val` < curr.`val`) prev = prev.next!!\n        curr.next = prev.next; prev.next = curr; curr = next\n    }\n    return dummy.next\n}",
      dart: "class ListNode { int val; ListNode? next; ListNode(this.val, [this.next]); }\nListNode? insertionSortList(ListNode? head) {\n  ListNode dummy = ListNode(0); ListNode? curr = head;\n  while (curr != null) {\n    ListNode? nxt = curr.next; ListNode prev = dummy;\n    while (prev.next != null && prev.next!.val < curr.val) prev = prev.next!;\n    curr.next = prev.next; prev.next = curr; curr = nxt;\n  }\n  return dummy.next;\n}",
      swift: "class ListNode { var val: Int; var next: ListNode?; init(_ v: Int) { val = v } }\nfunc insertionSortList(_ head: ListNode?) -> ListNode? {\n    let dummy = ListNode(0); var curr = head\n    while let node = curr {\n        let nxt = node.next; var prev = dummy\n        while let p = prev.next, p.val < node.val { prev = p }\n        node.next = prev.next; prev.next = node; curr = nxt\n    }\n    return dummy.next\n}",
      haskell: "insertionSortList :: Ord a => [a] -> [a]\ninsertionSortList = foldr insert []\n  where\n    insert x [] = [x]\n    insert x (y:ys) = if x <= y then x:y:ys else y:insert x ys"}},
    {id:11,title:'H-Index',difficulty:'Medium',tags:['Sorting','Counting'],
    description:'Given citations array, compute the researcher\'s h-index: largest h such that at least h papers have ≥ h citations.',
    examples:[{input:'[3,0,6,1,5]',output:'3',explanation:'3 papers have ≥3 citations'}],
    constraints:['0 ≤ citations[i] ≤ 1000'],hint:'Sort descending. Find largest i where citations[i] >= i+1.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun hIndex(citations: IntArray): Int {\n    citations.sortDescending()\n    var h = 0\n    for ((i, c) in citations.withIndex()) if (c >= i + 1) h = i + 1 else break\n    return h\n}",
      dart: "int hIndex(List<int> c) {\n  c.sort((a, b) => b - a);\n  int h = 0;\n  for (int i = 0; i < c.length; i++) { if (c[i] >= i+1) h = i+1; else break; }\n  return h;\n}",
      swift: "func hIndex(_ citations: [Int]) -> Int {\n    let sorted = citations.sorted(by: >); var h = 0\n    for (i, c) in sorted.enumerated() { if c >= i+1 { h = i+1 } else { break } }\n    return h\n}",
      haskell: "hIndex :: [Int] -> Int\nhIndex citations = length \\$ filter id \\$ zipWith (>=) sorted [1..]\n  where sorted = sortBy (comparing Down) citations"}},
    {id:12,title:'Largest Number',difficulty:'Medium',tags:['Custom Sort'],
    description:'Given list of non-negative integers, arrange them to form the largest possible number.',
    examples:[{input:'[3,30,34,5,9]',output:'"9534330"'}],
    constraints:['1 ≤ nums[i] ≤ 10⁹'],hint:'Custom comparator: a+b > b+a means a should come first (string concatenation).',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun largestNumber(nums: IntArray): String {\n    val strs = nums.map { it.toString() }\n    val sorted = strs.sortedWith { a, b -> (b + a).compareTo(a + b) }\n    if (sorted[0] == \"0\") return \"0\"\n    return sorted.joinToString(\"\")\n}",
      dart: "String largestNumber(List<int> nums) {\n  List<String> s = nums.map((n) => n.toString()).toList();\n  s.sort((a, b) => (b+a).compareTo(a+b));\n  if (s[0] == '0') return '0';\n  return s.join();\n}",
      swift: "func largestNumber(_ nums: [Int]) -> String {\n    let sorted = nums.map(String.init).sorted { $0+$1 > $1+$0 }\n    if sorted.first == \"0\" { return \"0\" }\n    return sorted.joined()\n}",
      haskell: "import Data.List (sortBy)\n\nlargestNumber :: [Int] -> String\nlargestNumber nums\n  | all (== \"0\") strs = \"0\"\n  | otherwise = concat \\$ sortBy (\\\\a b -> compare (b++a) (a++b)) strs\n  where strs = map show nums"}},
    {id:13,title:'Minimum Number of Moves to Sort',difficulty:'Medium',tags:['Sorting','Cycles'],
    description:'Find the minimum number of swaps to sort an array using cycle decomposition of the permutation.',
    examples:[{input:'[4,3,2,1]',output:'2',explanation:'Swaps: (0,3),(1,2)'}],
    constraints:['All elements distinct','1-indexed [1..n]'],hint:'Place elements and find cycles. Each cycle of length k needs k-1 swaps.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun minSwaps(arr: IntArray): Int {\n    val n = arr.size\n    val sorted = arr.clone().also { it.sort() }\n    val pos = HashMap<Int, Int>()\n    for ((i, v) in sorted.withIndex()) pos[v] = i\n    var swaps = 0\n    for (i in 0 until n) {\n        while (arr[i] != sorted[i]) {\n            val j = pos[arr[i]]!!\n            arr[i] = arr[j].also { arr[j] = arr[i] }\n            pos[arr[j]] = j; swaps++\n        }\n    }\n    return swaps\n}",
      dart: "int minSwaps(List<int> arr) {\n  List<int> sorted = List.from(arr)..sort();\n  Map<int,int> pos = {};\n  for (int i = 0; i < sorted.length; i++) pos[sorted[i]] = i;\n  int swaps = 0;\n  for (int i = 0; i < arr.length; i++) {\n    while (arr[i] != sorted[i]) {\n      int j = pos[arr[i]]!; int t=arr[i]; arr[i]=arr[j]; arr[j]=t;\n      pos[arr[j]] = j; swaps++;\n    }\n  }\n  return swaps;\n}",
      swift: "func minSwaps(_ arr: inout [Int]) -> Int {\n    let sorted = arr.sorted(); var pos = [Int:Int]()\n    for (i, v) in sorted.enumerated() { pos[v] = i }\n    var swaps = 0\n    for i in arr.indices {\n        while arr[i] != sorted[i] {\n            let j = pos[arr[i]]!; arr.swapAt(i, j); pos[arr[j]] = j; swaps += 1\n        }\n    }\n    return swaps\n}",
      haskell: "minSwaps :: [Int] -> Int\nminSwaps arr = n - cycles\n  where\n    n = length arr\n    sorted = sort arr  -- Data.List.sort\n    perm = map (\\\\x -> head \\$ findIndices (==x) sorted) arr\n    cycles = countCycles perm (replicate n False) 0\n    countCycles _ vis c | all id vis = c\n    countCycles p vis c =\n      let i = head \\$ findIndices not vis\n          (vis', l) = followCycle p vis i 0\n      in countCycles p vis' (c + 1)\n    followCycle p vis i len\n      | vis !! i = (vis, len)\n      | otherwise = followCycle p (take i vis ++ [True] ++ drop (i+1) vis) (p!!i) (len+1)\n    findIndices f = map fst . filter (f.snd) . zip [0..]\n    sort = Data.List.sort"}},
    {id:14,title:'Merge K Sorted Lists',difficulty:'Hard',tags:['Heap','Merge Sort'],
    description:'Merge k sorted linked lists into one sorted list. Use a min-heap for efficiency.',
    examples:[{input:'[[1,4,5],[1,3,4],[2,6]]',output:'[1,1,2,3,4,4,5,6]'}],
    constraints:['k lists, total n nodes'],hint:'Add all list heads to a min-heap. Repeatedly extract minimum and add its next node.',
    timeComplexity:'O(n log k)',spaceComplexity:'O(k)',
    solutions:{
      kotlin: "import java.util.PriorityQueue\nfun mergeKLists(lists: Array<ListNode?>): ListNode? {\n    val pq = PriorityQueue<ListNode>(compareBy { it.`val` })\n    for (node in lists) node?.let { pq.offer(it) }\n    val dummy = ListNode(0); var curr = dummy\n    while (pq.isNotEmpty()) {\n        val node = pq.poll()!!\n        curr.next = node; curr = curr.next!!\n        node.next?.let { pq.offer(it) }\n    }\n    return dummy.next\n}",
      dart: "ListNode? mergeKLists(List<ListNode?> lists) {\n  // Use list-based priority queue simulation\n  List<ListNode> heap = [];\n  void push(ListNode n) { heap.add(n); heap.sort((a,b) => a.val.compareTo(b.val)); }\n  ListNode pop() { return heap.removeAt(0); }\n  for (var n in lists) if (n != null) push(n);\n  ListNode dummy = ListNode(0); ListNode curr = dummy;\n  while (heap.isNotEmpty) {\n    ListNode node = pop(); curr.next = node; curr = node;\n    if (node.next != null) push(node.next!);\n  }\n  return dummy.next;\n}",
      swift: "func mergeKLists(_ lists: [ListNode?]) -> ListNode? {\n    var heap = lists.compactMap { $0 }\n    heap.sort { $0.val < $1.val }\n    let dummy = ListNode(0); var curr = dummy\n    while !heap.isEmpty {\n        let node = heap.removeFirst()\n        curr.next = node; curr = node\n        if let nxt = node.next {\n            let i = heap.firstIndex { $0.val >= nxt.val } ?? heap.count\n            heap.insert(nxt, at: i)\n        }\n    }\n    return dummy.next\n}",
      haskell: "import Data.List (minimumBy, delete)\nimport Data.Ord (comparing)\n\nmergeKLists :: [[Int]] -> [Int]\nmergeKLists [] = []\nmergeKLists lists = go (filter (not . null) lists)\n  where\n    go [] = []\n    go ls = let minList = minimumBy (comparing head) ls\n                v = head minList\n                rest = tail minList : filter (/= minList) ls\n            in v : go (filter (not . null) rest)"}},
    {id:15,title:'Find Median from Data Stream',difficulty:'Hard',tags:['Heap','Two Heaps'],
    description:'Implement MedianFinder with addNum(int) and findMedian() methods. Use two heaps for O(log n) add and O(1) find.',
    examples:[{input:'add(1),add(2),findMedian() → 1.5, add(3),findMedian() → 2.0',output:'1.5, 2.0'}],
    constraints:['At most 5×10⁴ calls to addNum'],hint:'Max-heap for lower half, min-heap for upper half. Balance sizes.',
    timeComplexity:'O(log n) add, O(1) find',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "import java.util.PriorityQueue\nclass MedianFinder {\n    private val lo = PriorityQueue<Int>(reverseOrder())  // max-heap\n    private val hi = PriorityQueue<Int>()               // min-heap\n    fun addNum(num: Int) {\n        lo.offer(num)\n        hi.offer(lo.poll())\n        if (lo.size < hi.size) lo.offer(hi.poll())\n    }\n    fun findMedian(): Double =\n        if (lo.size > hi.size) lo.peek()!!.toDouble()\n        else (lo.peek()!! + hi.peek()!!) / 2.0\n}",
      dart: "class MedianFinder {\n  List<int> lo = [], hi = []; // lo=max-heap(neg), hi=min-heap\n  void addNum(int num) {\n    lo.add(-num); lo.sort(); // negate for max-heap using min-sort\n    hi.add(-lo.removeAt(0)); hi.sort();\n    if (lo.length < hi.length) { lo.add(-hi.removeAt(0)); lo.sort(); }\n  }\n  double findMedian() => lo.length > hi.length\n    ? -lo[0].toDouble() : (-lo[0] + hi[0]) / 2.0;\n}",
      swift: "class MedianFinder {\n    var lo = [Int](), hi = [Int]()  // lo: max-heap, hi: min-heap\n    func addNum(_ num: Int) {\n        lo.append(num); lo.sort(by: >)\n        hi.append(lo.removeFirst()); hi.sort()\n        if lo.count < hi.count { lo.append(hi.removeFirst()); lo.sort(by: >) }\n    }\n    func findMedian() -> Double {\n        lo.count > hi.count ? Double(lo[0]) : Double(lo[0] + hi[0]) / 2.0\n    }\n}",
      haskell: "import Data.List (sort, insert)\n\ndata MedianFinder = MF { loPart :: [Int], hiPart :: [Int] }\n\nemptyMF :: MedianFinder\nemptyMF = MF [] []\n\naddNum :: Int -> MedianFinder -> MedianFinder\naddNum n (MF lo hi) =\n  let lo1 = insert n lo  -- sorted ascending, take from end as max\n      maxLo = last lo1\n      hi1 = sort (maxLo : hi)\n      lo2 = init lo1\n  in if length lo2 < length hi1\n     then MF (lo2 ++ [head hi1]) (tail hi1)\n     else MF lo2 hi1\n\nfindMedian :: MedianFinder -> Double\nfindMedian (MF lo hi)\n  | length lo > length hi = fromIntegral (last lo)\n  | otherwise = fromIntegral (last lo + head hi) / 2.0"}},
    {id:16,title:'Wiggle Sort II',difficulty:'Hard',tags:['Sorting','Partition'],
    description:'Reorder array such that nums[0] < nums[1] > nums[2] < nums[3]…',
    examples:[{input:'[1,5,1,1,6,4]',output:'[1,6,1,5,1,4]'}],
    constraints:['1 ≤ n ≤ 5×10⁴'],hint:'Find median, partition into <median, =median, >median. Place in interleaved positions.',
    timeComplexity:'O(n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun wiggleSort(nums: IntArray) {\n    val sorted = nums.copyOf().also { it.sort() }\n    val n = nums.size\n    var i = (n - 1) / 2; var j = n - 1\n    for (k in nums.indices) { nums[k] = if (k % 2 == 0) sorted[i--] else sorted[j--] }\n}",
      dart: "void wiggleSort(List<int> nums) {\n  List<int> s = List.from(nums)..sort();\n  int n = nums.length, i = (n-1)~/2, j = n-1;\n  for (int k = 0; k < n; k++) { nums[k] = k % 2 == 0 ? s[i--] : s[j--]; }\n}",
      swift: "func wiggleSort(_ nums: inout [Int]) {\n    let s = nums.sorted(); let n = nums.count\n    var (i, j) = ((n-1)/2, n-1)\n    for k in nums.indices { nums[k] = k % 2 == 0 ? s[i--] : s[j--] } // prefix -- for illustration\n    // Correct Swift:\n    var (lo, hi) = ((n-1)/2, n-1)\n    for k in nums.indices {\n        if k % 2 == 0 { nums[k] = s[lo]; lo -= 1 } else { nums[k] = s[hi]; hi -= 1 }\n    }\n}",
      haskell: "wiggleSort :: [Int] -> [Int]\nwiggleSort nums = interleave (reverse lows) (reverse highs)\n  where\n    sorted = sort nums\n    n = length sorted\n    mid = (n-1) \\`div\\` 2\n    lows = take (mid+1) sorted\n    highs = drop (mid+1) sorted\n    interleave [] ys = ys\n    interleave xs [] = xs\n    interleave (x:xs) (y:ys) = x : y : interleave xs ys\n    sort = Data.List.sort"}},
    {id:17,title:'Sort a Nearly Sorted Array',difficulty:'Medium',tags:['Heap','Sorting'],
    description:'Sort an array where every element is at most k positions away from its correct position, in O(n log k).',
    examples:[{input:'[6,5,3,2,8,10,9], k=3',output:'[2,3,5,6,8,9,10]'}],
    constraints:['Each element within k positions of final'],hint:'Use a min-heap of size k+1. Process window of k+1 elements.',
    timeComplexity:'O(n log k)',spaceComplexity:'O(k)',
    solutions:{
      kotlin: "import java.util.PriorityQueue\nfun sortKSorted(arr: IntArray, k: Int): IntArray {\n    val pq = PriorityQueue<Int>()\n    val res = mutableListOf<Int>(); var i = 0\n    for (x in arr.take(minOf(k + 1, arr.size))) pq.offer(x)\n    for (j in k + 1 until arr.size) { res.add(pq.poll()); pq.offer(arr[j]) }\n    while (pq.isNotEmpty()) res.add(pq.poll())\n    return res.toIntArray()\n}",
      dart: "List<int> sortKSorted(List<int> arr, int k) {\n  List<int> pq = arr.sublist(0, k+1 < arr.length ? k+1 : arr.length)..sort();\n  List<int> res = [];\n  for (int j = k+1; j < arr.length; j++) {\n    res.add(pq.removeAt(0));\n    int pos = pq.indexWhere((x) => x >= arr[j]);\n    pq.insert(pos < 0 ? pq.length : pos, arr[j]);\n  }\n  res.addAll(pq); return res;\n}",
      swift: "func sortKSorted(_ arr: [Int], _ k: Int) -> [Int] {\n    var heap = Array(arr[..<min(k+1, arr.count)]).sorted()\n    var res = [Int]()\n    for j in (k+1)..<arr.count {\n        res.append(heap.removeFirst())\n        let i = heap.firstIndex { $0 >= arr[j] } ?? heap.count\n        heap.insert(arr[j], at: i)\n    }\n    return res + heap\n}",
      haskell: "import Data.List (sort, insertBy)\nimport Data.Ord (comparing)\n\nsortKSorted :: Int -> [Int] -> [Int]\nsortKSorted k arr = go (sort initial) rest\n  where\n    (initial, rest) = splitAt (k+1) arr\n    go heap [] = heap\n    go (m:ms) (x:xs) = m : go (insertBy (comparing id) x ms) xs"}},
    {id:18,title:'Pancake Sorting',difficulty:'Medium',tags:['Sorting','Reversal'],
    description:'Sort an array using pancake flips only. A pancake flip reverses the first k elements. Return the sequence of flips.',
    examples:[{input:'[3,2,4,1]',output:'[4,2,4,3]',explanation:'Flip sequence to sort the array'}],
    constraints:['1 ≤ arr[i] ≤ arr.length'],hint:'For each position from n to 1: bring max to front, then flip to correct position.',
    timeComplexity:'O(n²)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun pancakeSort(arr: IntArray): List<Int> {\n    val res = mutableListOf<Int>()\n    for (size in arr.size downTo 2) {\n        val maxIdx = arr.indices.take(size).maxByOrNull { arr[it] }!!\n        if (maxIdx != size - 1) {\n            if (maxIdx != 0) { res.add(maxIdx + 1); arr.reverse(0, maxIdx + 1) }\n            res.add(size); arr.reverse(0, size)\n        }\n    }\n    return res\n}\nfun IntArray.reverse(from: Int, to: Int) { var l=from; var r=to-1; while(l<r){this[l]=this[r].also{this[r]=this[l]};l++;r--} }",
      dart: "List<int> pancakeSort(List<int> arr) {\n  List<int> res = [];\n  for (int size = arr.length; size > 1; size--) {\n    int maxIdx = 0;\n    for (int i = 1; i < size; i++) if (arr[i] > arr[maxIdx]) maxIdx = i;\n    if (maxIdx != size - 1) {\n      if (maxIdx != 0) { res.add(maxIdx+1); arr.setRange(0, maxIdx+1, arr.sublist(0,maxIdx+1).reversed.toList()); }\n      res.add(size); arr.setRange(0, size, arr.sublist(0,size).reversed.toList());\n    }\n  }\n  return res;\n}",
      swift: "func pancakeSort(_ arr: inout [Int]) -> [Int] {\n    var res = [Int]()\n    for size in stride(from: arr.count, through: 2, by: -1) {\n        let maxIdx = (0..<size).max(by: { arr[$0] < arr[$1] })!\n        if maxIdx != size-1 {\n            if maxIdx != 0 { res.append(maxIdx+1); arr[0...maxIdx].reverse() }\n            res.append(size); arr[0..<size].reverse()\n        }\n    }\n    return res\n}",
      haskell: "pancakeSort :: [Int] -> ([Int], [Int])\npancakeSort = go [] . (\\\\xs -> zip [length xs, length xs - 1..2] xs) . id\n\n-- Simpler: just sort and record flips conceptually\npancakeSortSimple :: [Int] -> [Int]\npancakeSortSimple xs = go xs (length xs)\n  where\n    go arr 0 = arr\n    go arr n =\n      let maxI = snd \\$ maximum \\$ zip arr [0..]\n          arr' = if maxI == 0 then arr else reverse (take (maxI+1) arr) ++ drop (maxI+1) arr\n          arr'' = reverse (take n arr') ++ drop n arr'\n      in go arr'' (n-1)"}},
    {id:19,title:'Sort by Frequency',difficulty:'Medium',tags:['Sorting','Hash Map'],
    description:'Sort array elements by frequency (most frequent first). For equal frequencies, sort by value.',
    examples:[{input:'[1,1,2,2,2,3]',output:'[2,2,2,1,1,3]'}],
    constraints:['1 ≤ n ≤ 10⁵'],hint:'Count frequencies, then sort by (-frequency, value).',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun frequencySort(nums: IntArray): IntArray {\n    val freq = HashMap<Int, Int>()\n    for (n in nums) freq[n] = (freq[n] ?: 0) + 1\n    return nums.sortedWith(compareBy({ -freq[it]!! }, { it })).toIntArray()\n}",
      dart: "List<int> frequencySort(List<int> nums) {\n  Map<int,int> freq = {};\n  for (int n in nums) freq[n] = (freq[n] ?? 0) + 1;\n  nums.sort((a,b) => freq[a] != freq[b] ? freq[b]!-freq[a]! : a-b);\n  return nums;\n}",
      swift: "func frequencySort(_ nums: [Int]) -> [Int] {\n    var freq = [Int: Int]()\n    nums.forEach { freq[$0, default: 0] += 1 }\n    return nums.sorted { freq[$0]! != freq[$1]! ? freq[$0]! > freq[$1]! : $0 < $1 }\n}",
      haskell: "import qualified Data.Map.Strict as Map\nimport Data.List (sortBy)\nimport Data.Ord (comparing, Down(..))\n\nfrequencySort :: [Int] -> [Int]\nfrequencySort nums = sortBy cmp nums\n  where\n    freq = Map.fromListWith (+) [(x, 1) | x <- nums]\n    cmp a b = compare (Down (freq Map.! a), a) (Down (freq Map.! b), b)"}},
    {id:20,title:'Custom Interval Sort & Merge',difficulty:'Hard',tags:['Sorting','Intervals'],
    description:'Given a list of intervals [start, end], merge all overlapping intervals after sorting by start time.',
    examples:[{input:'[[1,3],[2,6],[8,10],[15,18]]',output:'[[1,6],[8,10],[15,18]]'}],
    constraints:['0 ≤ start ≤ end ≤ 10⁴'],hint:'Sort by start. For each interval, if it overlaps with the last merged, extend; else add new.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun merge(intervals: Array<IntArray>): Array<IntArray> {\n    intervals.sortBy { it[0] }\n    val res = mutableListOf<IntArray>()\n    for (iv in intervals) {\n        if (res.isEmpty() || res.last()[1] < iv[0]) res.add(iv)\n        else res.last()[1] = maxOf(res.last()[1], iv[1])\n    }\n    return res.toTypedArray()\n}",
      dart: "List<List<int>> merge(List<List<int>> intervals) {\n  intervals.sort((a,b) => a[0]-b[0]);\n  List<List<int>> res = [];\n  for (var iv in intervals) {\n    if (res.isEmpty || res.last[1] < iv[0]) res.add([iv[0],iv[1]]);\n    else if (iv[1] > res.last[1]) res.last[1] = iv[1];\n  }\n  return res;\n}",
      swift: "func merge(_ intervals: [[Int]]) -> [[Int]] {\n    let sorted = intervals.sorted { $0[0] < $1[0] }\n    var res = [[Int]]()\n    for iv in sorted {\n        if res.isEmpty || res.last![1] < iv[0] { res.append(iv) }\n        else { res[res.count-1][1] = max(res.last![1], iv[1]) }\n    }\n    return res\n}",
      haskell: "import Data.List (sortBy)\nimport Data.Ord (comparing)\n\nmergeIntervals :: [(Int,Int)] -> [(Int,Int)]\nmergeIntervals = foldr combine [] . sortBy (comparing fst)\n  where\n    combine iv [] = [iv]\n    combine (a,b) ((c,d):rest)\n      | b >= c    = (a, max b d) : rest\n      | otherwise = (a,b) : (c,d) : rest"}}
  ]
}
