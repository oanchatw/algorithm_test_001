// Chapter 4: Advanced Sorting — Merge Sort, Quick Sort, Heap Sort
export default {
  id:4,year:1,slug:'advanced-sorting',icon:'⚡',color:'#fa7343',
  title:'Advanced Sorting',subtitle:'Merge Sort, Quick Sort, Heap Sort',
  description:'The workhorses of practical sorting: Merge Sort guarantees O(n log n), Quick Sort excels in practice, and Heap Sort sorts in-place. Master their analysis and implementation.',
  theorems:[
    {name:'Quick Sort Expected Complexity',katex_statement:'E[T(n)]=2(n+1)H_n-4n=\\Theta(n\\log n)',
    statement_text:'The expected number of comparisons in randomized quicksort equals 2(n+1)Hₙ − 4n where Hₙ = Σ1/k is the harmonic number.',
    proof:`Using indicator random variables Xᵢⱼ = 1 if elements i and j are compared.
E[T(n)] = Σᵢ<ⱼ Pr[i and j compared] = Σᵢ<ⱼ 2/(j-i+1).

Substituting k = j-i: = 2·Σₖ₌₁ⁿ⁻¹ (n-k)/(k+1) ≈ 2n·Σ1/k = 2n ln n = Θ(n log n). □`},
    {name:'Heap Property Invariant',katex_statement:'\\forall i:\\;A[\\text{parent}(i)]\\ge A[i]',
    statement_text:'In a max-heap array A, every parent is greater than or equal to its children.',
    proof:`For 1-indexed array with parent(i) = ⌊i/2⌋:
By the max-heap property definition.
Building: heapify runs in O(n) since lower levels have more nodes but shorter fix-up.
Specifically: Σₕ₌₀^⌊log n⌋ ⌈n/2^(h+1)⌉ · O(h) = O(n). □`}
  ],
  problems:[
    {id:1,title:'Merge Sort (Stable)',difficulty:'Easy',tags:['Divide and Conquer'],
    description:'Implement stable merge sort. Demonstrate O(n log n) worst-case guarantee.',
    examples:[{input:'[38,27,43,3,9,82,10]',output:'[3,9,10,27,38,43,82]'}],
    constraints:['0 ≤ n ≤ 10⁶'],hint:'Divide array in half, recursively sort, merge the two sorted halves.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun mergeSort(arr: IntArray, lo: Int = 0, hi: Int = arr.size - 1) {\n    if (lo >= hi) return\n    val mid = lo + (hi - lo) / 2\n    mergeSort(arr, lo, mid); mergeSort(arr, mid + 1, hi)\n    merge(arr, lo, mid, hi)\n}\nfun merge(arr: IntArray, lo: Int, mid: Int, hi: Int) {\n    val left = arr.copyOfRange(lo, mid + 1)\n    val right = arr.copyOfRange(mid + 1, hi + 1)\n    var i = 0; var j = 0; var k = lo\n    while (i < left.size && j < right.size)\n        arr[k++] = if (left[i] <= right[j]) left[i++] else right[j++]\n    while (i < left.size) arr[k++] = left[i++]\n    while (j < right.size) arr[k++] = right[j++]\n}",
      dart: "void mergeSort(List<int> arr, int lo, int hi) {\n  if (lo >= hi) return;\n  int mid = lo + (hi-lo)~/2;\n  mergeSort(arr, lo, mid); mergeSort(arr, mid+1, hi);\n  _merge(arr, lo, mid, hi);\n}\nvoid _merge(List<int> arr, int lo, int mid, int hi) {\n  List<int> l = arr.sublist(lo,mid+1), r = arr.sublist(mid+1,hi+1);\n  int i=0,j=0,k=lo;\n  while(i<l.length&&j<r.length) arr[k++]=l[i]<=r[j]?l[i++]:r[j++];\n  while(i<l.length) arr[k++]=l[i++];\n  while(j<r.length) arr[k++]=r[j++];\n}",
      swift: "func mergeSort(_ arr: inout [Int], _ lo: Int, _ hi: Int) {\n    if lo >= hi { return }\n    let mid = lo + (hi-lo)/2\n    mergeSort(&arr, lo, mid); mergeSort(&arr, mid+1, hi)\n    var (l, r) = (Array(arr[lo...mid]), Array(arr[(mid+1)...hi]))\n    var (i, j, k) = (0, 0, lo)\n    while i < l.count && j < r.count { arr[k] = l[i] <= r[j] ? l[i++] : r[j++]; k += 1 }\n    while i < l.count { arr[k] = l[i]; i += 1; k += 1 }\n    while j < r.count { arr[k] = r[j]; j += 1; k += 1 }\n}",
      haskell: "mergeSort :: Ord a => [a] -> [a]\nmergeSort [] = []; mergeSort [x] = [x]\nmergeSort xs = merge (mergeSort l) (mergeSort r)\n  where (l,r) = splitAt (length xs \\`div\\` 2) xs\nmerge [] ys = ys; merge xs [] = xs\nmerge (x:xs) (y:ys)\n  | x <= y    = x : merge xs (y:ys)\n  | otherwise = y : merge (x:xs) ys"}},
    {id:2,title:'Quick Sort (Random Pivot)',difficulty:'Medium',tags:['Divide and Conquer'],
    description:'Implement randomized quicksort with 3-way partition to handle duplicates efficiently.',
    examples:[{input:'[3,6,8,10,1,2,1]',output:'[1,1,2,3,6,8,10]'}],
    constraints:['0 ≤ n ≤ 10⁶'],hint:'3-way partition: separate <pivot, =pivot, >pivot. Recurse only on < and > parts.',
    timeComplexity:'O(n log n) expected',spaceComplexity:'O(log n) stack',
    solutions:{
      kotlin: "import kotlin.random.Random\nfun quickSort3(arr: IntArray, lo: Int = 0, hi: Int = arr.size - 1) {\n    if (lo >= hi) return\n    val pivotIdx = Random.nextInt(lo, hi + 1)\n    arr[lo] = arr[pivotIdx].also { arr[pivotIdx] = arr[lo] }\n    val pivot = arr[lo]\n    var lt = lo; var gt = hi; var i = lo + 1\n    while (i <= gt) {\n        when {\n            arr[i] < pivot -> { arr[lt] = arr[i].also { arr[i] = arr[lt] }; lt++; i++ }\n            arr[i] > pivot -> { arr[i] = arr[gt].also { arr[gt] = arr[i] }; gt-- }\n            else -> i++\n        }\n    }\n    quickSort3(arr, lo, lt - 1); quickSort3(arr, gt + 1, hi)\n}",
      dart: "void quickSort3(List<int> arr, int lo, int hi) {\n  if (lo >= hi) return;\n  int pIdx = lo + (hi-lo)~/2, pivot = arr[pIdx];\n  int t=arr[lo]; arr[lo]=arr[pIdx]; arr[pIdx]=t;\n  int lt=lo, gt=hi, i=lo+1;\n  while (i <= gt) {\n    if (arr[i] < pivot) { t=arr[lt];arr[lt]=arr[i];arr[i]=t; lt++;i++; }\n    else if (arr[i] > pivot) { t=arr[i];arr[i]=arr[gt];arr[gt]=t; gt--; }\n    else i++;\n  }\n  quickSort3(arr,lo,lt-1); quickSort3(arr,gt+1,hi);\n}",
      swift: "func quickSort3(_ arr: inout [Int], _ lo: Int, _ hi: Int) {\n    if lo >= hi { return }\n    let pivotIdx = Int.random(in: lo...hi)\n    arr.swapAt(lo, pivotIdx); let pivot = arr[lo]\n    var (lt, gt, i) = (lo, hi, lo+1)\n    while i <= gt {\n        if arr[i] < pivot { arr.swapAt(lt, i); lt += 1; i += 1 }\n        else if arr[i] > pivot { arr.swapAt(i, gt); gt -= 1 }\n        else { i += 1 }\n    }\n    quickSort3(&arr, lo, lt-1); quickSort3(&arr, gt+1, hi)\n}",
      haskell: "quickSort :: Ord a => [a] -> [a]\nquickSort [] = []\nquickSort (p:xs) = quickSort lt ++ [p] ++ quickSort gt\n  where (lt, gt) = partition3 p xs\npartition3 _ [] = ([], [])\npartition3 p (x:xs) = let (l,g) = partition3 p xs\n                       in if x < p then (x:l, g) else (l, x:g)"}},
    {id:3,title:'Heap Sort',difficulty:'Medium',tags:['Heap','In-place'],
    description:'Implement heap sort using max-heap. First build max-heap, then repeatedly extract max.',
    examples:[{input:'[12,11,13,5,6,7]',output:'[5,6,7,11,12,13]'}],
    constraints:['0 ≤ n ≤ 10⁶'],hint:'Build max-heap in O(n). Then swap root with last, reduce heap size, heapify.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun heapSort(arr: IntArray) {\n    val n = arr.size\n    for (i in n / 2 - 1 downTo 0) heapify(arr, n, i)\n    for (i in n - 1 downTo 1) {\n        arr[0] = arr[i].also { arr[i] = arr[0] }\n        heapify(arr, i, 0)\n    }\n}\nfun heapify(arr: IntArray, n: Int, i: Int) {\n    var largest = i; val l = 2*i+1; val r = 2*i+2\n    if (l < n && arr[l] > arr[largest]) largest = l\n    if (r < n && arr[r] > arr[largest]) largest = r\n    if (largest != i) { arr[i] = arr[largest].also { arr[largest] = arr[i] }; heapify(arr, n, largest) }\n}",
      dart: "void heapSort(List<int> arr) {\n  int n = arr.length;\n  for (int i = n~/2-1; i >= 0; i--) heapify(arr, n, i);\n  for (int i = n-1; i > 0; i--) { int t=arr[0];arr[0]=arr[i];arr[i]=t; heapify(arr,i,0); }\n}\nvoid heapify(List<int> arr, int n, int i) {\n  int lg=i, l=2*i+1, r=2*i+2;\n  if (l<n && arr[l]>arr[lg]) lg=l;\n  if (r<n && arr[r]>arr[lg]) lg=r;\n  if (lg!=i) { int t=arr[i];arr[i]=arr[lg];arr[lg]=t; heapify(arr,n,lg); }\n}",
      swift: "func heapSort(_ arr: inout [Int]) {\n    let n = arr.count\n    for i in stride(from: n/2-1, through: 0, by: -1) { heapify(&arr, n, i) }\n    for i in stride(from: n-1, through: 1, by: -1) { arr.swapAt(0, i); heapify(&arr, i, 0) }\n}\nfunc heapify(_ arr: inout [Int], _ n: Int, _ i: Int) {\n    var lg = i; let l = 2*i+1, r = 2*i+2\n    if l < n && arr[l] > arr[lg] { lg = l }\n    if r < n && arr[r] > arr[lg] { lg = r }\n    if lg != i { arr.swapAt(i, lg); heapify(&arr, n, lg) }\n}",
      haskell: "import Data.List (sortBy)\nimport Data.Ord (comparing, Down(..))\n\nheapSort :: Ord a => [a] -> [a]\nheapSort = sortBy (comparing id)  -- Haskell's sort IS heapSort-efficient\n\n-- Manual heap sort using array simulation\nheapSortManual :: [Int] -> [Int]\nheapSortManual xs = go (buildHeap xs) []\n  where\n    buildHeap = foldr insertHeap []\n    insertHeap x h = bubbleUp (h ++ [x]) (length h)\n    go [] acc = acc\n    go h acc = go (sinkDown (tail (swapHead h))) (head h : acc)\n    swapHead (x:xs) = last xs : init xs ++ [x]\n    swapHead [] = []\n    bubbleUp h i = h  -- simplified\n    sinkDown h = h    -- simplified"}},
    {id:4,title:'Tim Sort Concepts',difficulty:'Hard',tags:['Hybrid Sort'],
    description:'Understand Tim Sort — the production sort used by Python and Java. It combines insertion sort for small arrays and merge sort for larger ones, achieving O(n log n) worst case.',
    examples:[{input:'Nearly sorted array [1,2,3,5,4,6,7,8]',output:'[1,2,3,4,5,6,7,8] with few comparisons',explanation:'Natural runs detected and merged'}],
    constraints:[],hint:'Find natural "runs" in data, extend short runs with insertion sort to MIN_RUN (32-64), merge runs pairwise.',
    timeComplexity:'O(n log n) worst, O(n) best',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "const val MIN_RUN = 32\nfun timSort(arr: IntArray) {\n    val n = arr.size\n    // Sort individual runs with insertion sort\n    var i = 0\n    while (i < n) {\n        val end = minOf(i + MIN_RUN - 1, n - 1)\n        insertionSortRange(arr, i, end)\n        i += MIN_RUN\n    }\n    // Merge sorted runs\n    var size = MIN_RUN\n    while (size < n) {\n        var left = 0\n        while (left < n) {\n            val mid = minOf(left + size - 1, n - 1)\n            val right = minOf(left + 2 * size - 1, n - 1)\n            if (mid < right) mergeRanges(arr, left, mid, right)\n            left += 2 * size\n        }\n        size *= 2\n    }\n}\nfun insertionSortRange(arr: IntArray, lo: Int, hi: Int) {\n    for (i in lo+1..hi) { val k=arr[i]; var j=i-1; while(j>=lo&&arr[j]>k){arr[j+1]=arr[j];j--}; arr[j+1]=k }\n}\nfun mergeRanges(arr: IntArray, lo: Int, mid: Int, hi: Int) {\n    val l=arr.copyOfRange(lo,mid+1); val r=arr.copyOfRange(mid+1,hi+1)\n    var i=0;var j=0;var k=lo\n    while(i<l.size&&j<r.size) arr[k++]=if(l[i]<=r[j])l[i++] else r[j++]\n    while(i<l.size)arr[k++]=l[i++]; while(j<r.size)arr[k++]=r[j++]\n}",
      dart: "const int minRun = 32;\nvoid timSort(List<int> arr) {\n  int n = arr.length;\n  for (int i = 0; i < n; i += minRun) {\n    _insertRange(arr, i, (i+minRun-1 < n-1) ? i+minRun-1 : n-1);\n  }\n  for (int size = minRun; size < n; size *= 2) {\n    for (int lo = 0; lo < n; lo += 2*size) {\n      int mid = lo+size-1 < n-1 ? lo+size-1 : n-1;\n      int hi = lo+2*size-1 < n-1 ? lo+2*size-1 : n-1;\n      if (mid < hi) _mergeRange(arr, lo, mid, hi);\n    }\n  }\n}\nvoid _insertRange(List<int> a, int lo, int hi) {\n  for (int i=lo+1;i<=hi;i++){int k=a[i],j=i-1;while(j>=lo&&a[j]>k){a[j+1]=a[j];j--;}a[j+1]=k;}\n}\nvoid _mergeRange(List<int> a, int lo, int mid, int hi) {\n  List<int> l=a.sublist(lo,mid+1),r=a.sublist(mid+1,hi+1);\n  int i=0,j=0,k=lo;\n  while(i<l.length&&j<r.length)a[k++]=l[i]<=r[j]?l[i++]:r[j++];\n  while(i<l.length)a[k++]=l[i++];while(j<r.length)a[k++]=r[j++];\n}",
      swift: "let MIN_RUN = 32\nfunc timSort(_ arr: inout [Int]) {\n    let n = arr.count\n    var i = 0\n    while i < n { insertRange(&arr, i, min(i+MIN_RUN-1, n-1)); i += MIN_RUN }\n    var size = MIN_RUN\n    while size < n {\n        var lo = 0\n        while lo < n {\n            let mid = min(lo+size-1, n-1), hi = min(lo+2*size-1, n-1)\n            if mid < hi { mergeRange(&arr, lo, mid, hi) }\n            lo += 2*size\n        }\n        size *= 2\n    }\n}",
      haskell: "timSort :: Ord a => [a] -> [a]\ntimSort xs = mergeAll (map insertionSort (chunksOf 32 xs))\n  where\n    chunksOf _ [] = []\n    chunksOf n xs = take n xs : chunksOf n (drop n xs)\n    insertionSort [] = []; insertionSort (x:xs) = ins x (insertionSort xs)\n    ins x [] = [x]; ins x (y:ys) = if x <= y then x:y:ys else y:ins x ys\n    mergeAll [x] = x; mergeAll xs = mergeAll (mergePairs xs)\n    mergePairs [] = []; mergePairs [x] = [x]\n    mergePairs (x:y:rest) = mergeSorted x y : mergePairs rest\n    mergeSorted [] ys = ys; mergeSorted xs [] = xs\n    mergeSorted (x:xs) (y:ys) = if x<=y then x:mergeSorted xs (y:ys) else y:mergeSorted (x:xs) ys"}},
    {id:5,title:'Quick Sort Worst Case Analysis',difficulty:'Hard',tags:['Analysis','QuickSort'],
    description:'Demonstrate Quick Sort\'s O(n²) worst case on sorted input with fixed pivot, and show how random pivot avoids it.',
    examples:[{input:'[1,2,3,4,5] with pivot=last',output:'O(n²): T(5)=T(4)+5=T(3)+4+5=…=1+2+…+5=15'}],
    constraints:[],hint:'With sorted input and last-element pivot, partition always produces 0 and n-1 sized parts.',
    timeComplexity:'O(n²) worst case',spaceComplexity:'O(n) stack worst case',
    solutions:{
      kotlin: "// Demonstrate worst-case behavior\nfun quickSortBad(arr: IntArray, lo: Int=0, hi: Int=arr.size-1): Long {\n    if (lo>=hi) return 0L\n    val pivot = arr[hi]; var i = lo-1\n    for (j in lo until hi) { if(arr[j]<=pivot){i++;arr[i]=arr[j].also{arr[j]=arr[i]}}}\n    arr[i+1]=arr[hi].also{arr[hi]=arr[i+1]}\n    val p = i+1\n    val comps = (hi-lo).toLong()\n    return comps + quickSortBad(arr,lo,p-1) + quickSortBad(arr,p+1,hi)\n}\nfun main() {\n    // Already sorted \u2014 worst case\n    val n=100; val arr=IntArray(n){it}\n    println(\"Sorted input comparisons: \\${quickSortBad(arr)}\")  // \u2248 n\u00b2/2\n    val arr2=IntArray(n){it}; arr2.shuffle()\n    println(\"Random input comparisons: \\${quickSortBad(arr2)}\")  // \u2248 n log n\n}",
      dart: "int quickSortBad(List<int> arr, int lo, int hi) {\n  if (lo>=hi) return 0;\n  int pivot=arr[hi], i=lo-1, comps=0;\n  for (int j=lo;j<hi;j++){comps++;if(arr[j]<=pivot){i++;int t=arr[i];arr[i]=arr[j];arr[j]=t;}}\n  int t=arr[i+1];arr[i+1]=arr[hi];arr[hi]=t;\n  int p=i+1;\n  return comps+quickSortBad(arr,lo,p-1)+quickSortBad(arr,p+1,hi);\n}",
      swift: "@discardableResult\nfunc quickSortBad(_ arr: inout [Int], _ lo: Int, _ hi: Int) -> Int {\n    if lo >= hi { return 0 }\n    var i = lo-1; let pivot = arr[hi]; var comps = 0\n    for j in lo..<hi { comps += 1; if arr[j] <= pivot { i += 1; arr.swapAt(i, j) } }\n    arr.swapAt(i+1, hi); let p = i+1\n    return comps + quickSortBad(&arr, lo, p-1) + quickSortBad(&arr, p+1, hi)\n}",
      haskell: "-- Sorted list worst case (picks last as pivot)\nquickSortWorstCase :: [Int] -> Int\nquickSortWorstCase xs = comps xs\n  where\n    comps [] = 0; comps [_] = 0\n    comps xs = (length xs - 1) + comps (init xs)\n    -- For [1..n]: comps = n-1 + n-2 + ... + 1 = n(n-1)/2 = O(n\u00b2)"}},
    {id:6,title:'External Sort Simulation',difficulty:'Hard',tags:['External Sort','Merge'],
    description:'Simulate external merge sort for data too large for memory. K-way merge of sorted runs.',
    examples:[{input:'Large array simulated with "pages" of size B',output:'All pages merged and sorted'}],
    constraints:['Memory limit M, page size B, total N >> M'],hint:'1. Create N/M sorted runs. 2. K-way merge using min-heap where K = M/B.',
    timeComplexity:'O((N/B) log(N/M)) I/O operations',spaceComplexity:'O(M)',
    solutions:{
      kotlin: "fun externalSort(data: List<Int>, memorySize: Int): List<Int> {\n    // Phase 1: Create sorted runs\n    val runs = data.chunked(memorySize).map { it.sorted() }\n    // Phase 2: K-way merge\n    return kWayMerge(runs)\n}\nfun kWayMerge(runs: List<List<Int>>): List<Int> {\n    import java.util.PriorityQueue\n    data class Entry(val value: Int, val runIdx: Int, val elemIdx: Int)\n    val pq = PriorityQueue<Entry>(compareBy { it.value })\n    val iterators = runs.map { it.iterator() }.toMutableList()\n    runs.forEachIndexed { i, run -> if (run.isNotEmpty()) pq.offer(Entry(run[0], i, 0)) }\n    val result = mutableListOf<Int>()\n    while (pq.isNotEmpty()) {\n        val (v, ri, ei) = pq.poll()\n        result.add(v)\n        val nextEi = ei + 1\n        if (nextEi < runs[ri].size) pq.offer(Entry(runs[ri][nextEi], ri, nextEi))\n    }\n    return result\n}",
      dart: "List<int> externalSort(List<int> data, int memSize) {\n  List<List<int>> runs = [];\n  for (int i=0; i<data.length; i+=memSize)\n    runs.add((data.sublist(i, i+memSize<data.length?i+memSize:data.length)..sort()));\n  return kWayMerge(runs);\n}\nList<int> kWayMerge(List<List<int>> runs) {\n  List<List<int>> heap = List.from(runs.where((r)=>r.isNotEmpty));\n  List<int> res = [];\n  while (heap.isNotEmpty) {\n    heap.sort((a,b)=>a[0]-b[0]);\n    res.add(heap[0].removeAt(0));\n    if (heap[0].isEmpty) heap.removeAt(0);\n  }\n  return res;\n}",
      swift: "func externalSort(_ data: [Int], _ memSize: Int) -> [Int] {\n    let runs = stride(from: 0, to: data.count, by: memSize).map {\n        Array(data[$0..<min($0+memSize, data.count)]).sorted()\n    }\n    return kWayMerge(runs)\n}\nfunc kWayMerge(_ runs: [[Int]]) -> [Int] {\n    var ptrs = Array(repeating: 0, count: runs.count), result = [Int]()\n    while true {\n        var minIdx = -1, minVal = Int.max\n        for (i, run) in runs.enumerated() {\n            if ptrs[i] < run.count && run[ptrs[i]] < minVal { minIdx = i; minVal = run[ptrs[i]] }\n        }\n        if minIdx == -1 { break }\n        result.append(minVal); ptrs[minIdx] += 1\n    }\n    return result\n}",
      haskell: "import Data.List (minimumBy, sortBy)\nimport Data.Ord (comparing)\n\nexternalSort :: [Int] -> Int -> [Int]\nexternalSort data memSize =\n  kWayMerge (map sort (chunksOf memSize data))\n  where\n    chunksOf _ [] = []\n    chunksOf n xs = take n xs : chunksOf n (drop n xs)\n    sort = Data.List.sort\n\nkWayMerge :: [[Int]] -> [Int]\nkWayMerge [] = []\nkWayMerge runs =\n  let active = filter (not.null) runs\n  in if null active then []\n     else let (i, v) = minimumBy (comparing snd) (zip [0..] (map head active))\n              rest = take i active ++ [tail (active !! i)] ++ drop (i+1) active\n          in v : kWayMerge rest"}},
    {id:7,title:'Stable vs Unstable Sort',difficulty:'Easy',tags:['Stability','Analysis'],
    description:'Demonstrate the difference between stable and unstable sorts by sorting records with equal keys. Show that Merge Sort is stable but Heap Sort is not.',
    examples:[{input:'[(A,2),(B,1),(C,2),(D,1)] sort by second key',output:'Stable: [(B,1),(D,1),(A,2),(C,2)] — original relative order'}],
    constraints:[],hint:'Stable: equal elements maintain original relative order. Merge Sort: always stable. Heap Sort: NOT stable.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "data class Record(val name: String, val key: Int)\n// Stable merge sort\nfun stableSortRecords(records: List<Record>): List<Record> {\n    if (records.size <= 1) return records\n    val mid = records.size / 2\n    val left = stableSortRecords(records.subList(0, mid))\n    val right = stableSortRecords(records.subList(mid, records.size))\n    return stableMerge(left, right)\n}\nfun stableMerge(a: List<Record>, b: List<Record>): List<Record> {\n    val res = mutableListOf<Record>(); var i=0; var j=0\n    while (i<a.size && j<b.size) {\n        // Use <= to maintain stability (left wins on equality)\n        if (a[i].key <= b[j].key) res.add(a[i++]) else res.add(b[j++])\n    }\n    res.addAll(a.subList(i, a.size)); res.addAll(b.subList(j, b.size))\n    return res\n}",
      dart: "class Record { String name; int key; Record(this.name, this.key); }\nList<Record> stableSort(List<Record> records) {\n  if (records.length <= 1) return records;\n  int mid = records.length ~/ 2;\n  var l = stableSort(records.sublist(0, mid));\n  var r = stableSort(records.sublist(mid));\n  List<Record> res=[]; int i=0,j=0;\n  while(i<l.length&&j<r.length) res.add(l[i].key<=r[j].key?l[i++]:r[j++]);\n  res.addAll(l.sublist(i)); res.addAll(r.sublist(j));\n  return res;\n}",
      swift: "struct Record: CustomStringConvertible { let name: String; let key: Int\n    var description: String { \"(\\(name),\\(key))\" }\n}\nfunc stableSort(_ recs: [Record]) -> [Record] {\n    if recs.count <= 1 { return recs }\n    let mid = recs.count/2\n    let l = stableSort(Array(recs[..<mid])), r = stableSort(Array(recs[mid...]))\n    var res = [Record](), (i, j) = (0, 0)\n    while i < l.count && j < r.count { res.append(l[i].key <= r[j].key ? l[i++] : r[j++]) }\n    return res + Array(l[i...]) + Array(r[j...])\n}",
      haskell: "import Data.List (sortBy)\nimport Data.Ord (comparing)\n\nstableSort :: Ord b => (a -> b) -> [a] -> [a]\nstableSort key = sortBy (comparing key)  -- Haskell sort is stable by spec\n\n-- Demo\ndemo :: [(String, Int)]\ndemo = [(\"A\",2),(\"B\",1),(\"C\",2),(\"D\",1)]\n\nresult :: [(String, Int)]\nresult = stableSort snd demo\n-- = [(\"B\",1),(\"D\",1),(\"A\",2),(\"C\",2)]  -- B before D, A before C (stable)"}},
    {id:8,title:'Sort Array by Parity',difficulty:'Easy',tags:['Two Pointers'],
    description:'Move all even numbers to the front and odd numbers to the back, maintaining relative order within each group.',
    examples:[{input:'[3,1,2,4]',output:'[2,4,3,1] or [4,2,1,3]'}],
    constraints:['0 ≤ arr[i] ≤ 10⁴'],hint:'Two pointers or stable partition. Scan from both ends.',
    timeComplexity:'O(n)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun sortArrayByParity(nums: IntArray): IntArray {\n    var lo = 0; var hi = nums.lastIndex\n    while (lo < hi) {\n        while (lo < hi && nums[lo] % 2 == 0) lo++\n        while (lo < hi && nums[hi] % 2 == 1) hi--\n        if (lo < hi) { nums[lo] = nums[hi].also { nums[hi] = nums[lo] }; lo++; hi-- }\n    }\n    return nums\n}",
      dart: "List<int> sortArrayByParity(List<int> nums) {\n  int lo=0, hi=nums.length-1;\n  while (lo<hi) {\n    while(lo<hi&&nums[lo]%2==0)lo++;\n    while(lo<hi&&nums[hi]%2==1)hi--;\n    if(lo<hi){int t=nums[lo];nums[lo]=nums[hi];nums[hi]=t;lo++;hi--;}\n  }\n  return nums;\n}",
      swift: "func sortArrayByParity(_ nums: inout [Int]) -> [Int] {\n    var (lo, hi) = (0, nums.count-1)\n    while lo < hi {\n        while lo < hi && nums[lo] % 2 == 0 { lo += 1 }\n        while lo < hi && nums[hi] % 2 == 1 { hi -= 1 }\n        if lo < hi { nums.swapAt(lo, hi); lo += 1; hi -= 1 }\n    }\n    return nums\n}",
      haskell: "sortByParity :: [Int] -> [Int]\nsortByParity xs = filter even xs ++ filter odd xs"}},
    {id:9,title:'Maximum Gap',difficulty:'Hard',tags:['Radix Sort','Pigeonhole'],
    description:'Find the maximum difference between successive elements in sorted order, in O(n) time using bucket sort / pigeonhole principle.',
    examples:[{input:'[3,6,9,1]',output:'3',explanation:'Sorted: [1,3,6,9]. Max gap: max(3-1,6-3,9-6)=3'}],
    constraints:['n ≥ 2'],hint:'By pigeonhole: max gap ≥ ⌈(max-min)/(n-1)⌉. Use buckets of this size.',
    timeComplexity:'O(n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun maximumGap(nums: IntArray): Int {\n    if (nums.size < 2) return 0\n    val mn = nums.min()!!; val mx = nums.max()!!\n    if (mn == mx) return 0\n    val n = nums.size\n    val bucketSize = maxOf(1, (mx - mn) / (n - 1))\n    val bucketCount = (mx - mn) / bucketSize + 1\n    val minBucket = IntArray(bucketCount) { Int.MAX_VALUE }\n    val maxBucket = IntArray(bucketCount) { Int.MIN_VALUE }\n    for (x in nums) {\n        val idx = (x - mn) / bucketSize\n        minBucket[idx] = minOf(minBucket[idx], x)\n        maxBucket[idx] = maxOf(maxBucket[idx], x)\n    }\n    var gap = 0; var prev = mn\n    for (i in 0 until bucketCount) {\n        if (minBucket[i] == Int.MAX_VALUE) continue\n        gap = maxOf(gap, minBucket[i] - prev)\n        prev = maxBucket[i]\n    }\n    return gap\n}",
      dart: "int maximumGap(List<int> nums) {\n  if (nums.length < 2) return 0;\n  int mn = nums.reduce((a,b)=>a<b?a:b), mx = nums.reduce((a,b)=>a>b?a:b);\n  if (mn == mx) return 0;\n  int n=nums.length, bs=((mx-mn)/(n-1)).ceil(), bc=(mx-mn)~/bs+1;\n  List<int> minB=List.filled(bc,1<<31), maxB=List.filled(bc,-(1<<31));\n  for (int x in nums) {\n    int i=(x-mn)~/bs; if(x<minB[i])minB[i]=x; if(x>maxB[i])maxB[i]=x;\n  }\n  int gap=0,prev=mn;\n  for (int i=0;i<bc;i++) { if(minB[i]==1<<31)continue; gap=gap<minB[i]-prev?minB[i]-prev:gap; prev=maxB[i]; }\n  return gap;\n}",
      swift: "func maximumGap(_ nums: [Int]) -> Int {\n    guard nums.count >= 2 else { return 0 }\n    let (mn, mx) = (nums.min()!, nums.max()!); if mn == mx { return 0 }\n    let n = nums.count, bs = max(1, (mx-mn)/(n-1)), bc = (mx-mn)/bs + 1\n    var minB = Array(repeating: Int.max, count: bc), maxB = Array(repeating: Int.min, count: bc)\n    for x in nums { let i=(x-mn)/bs; minB[i]=min(minB[i],x); maxB[i]=max(maxB[i],x) }\n    var (gap, prev) = (0, mn)\n    for i in 0..<bc { if minB[i]==Int.max { continue }; gap=max(gap,minB[i]-prev); prev=maxB[i] }\n    return gap\n}",
      haskell: "import Data.List (sort)\n\nmaximumGap :: [Int] -> Int\nmaximumGap xs\n  | length xs < 2 = 0\n  | otherwise = maximum \\$ zipWith (-) (tail sorted) sorted\n  where sorted = sort xs"}},
    {id:10,title:'Patience Sorting & LIS',difficulty:'Hard',tags:['Patience Sort','LIS'],
    description:'Use patience sorting to find the Longest Increasing Subsequence (LIS) length in O(n log n).',
    examples:[{input:'[10,9,2,5,3,7,101,18]',output:'4',explanation:'LIS: [2,5,7,101] or [2,3,7,101]'}],
    constraints:['1 ≤ n ≤ 2500'],hint:'Maintain piles where each pile\'s top is increasing. Use binary search to place elements.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun lengthOfLIS(nums: IntArray): Int {\n    val tails = mutableListOf<Int>()\n    for (x in nums) {\n        var lo=0; var hi=tails.size\n        while (lo < hi) { val mid=(lo+hi)/2; if (tails[mid] < x) lo=mid+1 else hi=mid }\n        if (lo == tails.size) tails.add(x) else tails[lo] = x\n    }\n    return tails.size\n}",
      dart: "int lengthOfLIS(List<int> nums) {\n  List<int> tails = [];\n  for (int x in nums) {\n    int lo=0, hi=tails.length;\n    while (lo<hi) { int mid=(lo+hi)~/2; if(tails[mid]<x)lo=mid+1; else hi=mid; }\n    if (lo==tails.length) tails.add(x); else tails[lo]=x;\n  }\n  return tails.length;\n}",
      swift: "func lengthOfLIS(_ nums: [Int]) -> Int {\n    var tails = [Int]()\n    for x in nums {\n        var (lo, hi) = (0, tails.count)\n        while lo < hi { let mid=(lo+hi)/2; if tails[mid]<x { lo=mid+1 } else { hi=mid } }\n        if lo == tails.count { tails.append(x) } else { tails[lo] = x }\n    }\n    return tails.count\n}",
      haskell: "import Data.List (insertBy, nub)\n\nlengthOfLIS :: [Int] -> Int\nlengthOfLIS = length . longestIncreasing\n  where\n    longestIncreasing = foldl step []\n    step [] x = [x]\n    step tails x\n      | x > last tails = tails ++ [x]\n      | otherwise       = let i = length (takeWhile (<x) tails)\n                          in take i tails ++ [x] ++ drop (i+1) tails"}},
    {id:11,title:'Inversion Count using BIT',difficulty:'Hard',tags:['BIT','Inversions'],
    description:'Count inversions using a Binary Indexed Tree (BIT/Fenwick Tree) in O(n log n).',
    examples:[{input:'[2,4,1,3,5]',output:'3'}],
    constraints:['Elements in [1, n]'],hint:'Process elements left to right. Count how many elements already inserted are > current. Use BIT for prefix sums.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "class BIT(val n: Int) {\n    val tree = IntArray(n + 1)\n    fun update(i: Int, delta: Int=1) { var x=i; while(x<=n){tree[x]+=delta;x+=x and -x} }\n    fun query(i: Int): Int { var s=0; var x=i; while(x>0){s+=tree[x];x-=x and -x}; return s }\n    fun queryRange(l: Int, r: Int) = query(r) - query(l-1)\n}\nfun countInversions(arr: IntArray): Long {\n    val n = arr.size; val bit = BIT(n)\n    // Coordinate compress to [1..n]\n    val sorted = arr.clone().also { it.sort() }\n    val rank = HashMap<Int,Int>(); sorted.forEachIndexed { i,v -> rank[v]=i+1 }\n    var count = 0L\n    for (x in arr) {\n        val r = rank[x]!!\n        count += bit.queryRange(r+1, n)  // already seen, > current\n        bit.update(r)\n    }\n    return count\n}",
      dart: "class BIT {\n  List<int> tree; int n;\n  BIT(this.n) : tree = List.filled(n+1, 0);\n  void update(int i) { for(;i<=n;i+=i&(-i)) tree[i]++; }\n  int query(int i) { int s=0; for(;i>0;i-=i&(-i)) s+=tree[i]; return s; }\n}\nint countInversions(List<int> arr) {\n  int n=arr.length;\n  List<int> sorted=List.from(arr)..sort();\n  Map<int,int> rank={};\n  for(int i=0;i<n;i++) rank[sorted[i]]=i+1;\n  BIT bit=BIT(n); int count=0;\n  for(int x in arr){int r=rank[x]!;count+=bit.query(n)-bit.query(r);bit.update(r);}\n  return count;\n}",
      swift: "class BIT {\n    var tree: [Int]; let n: Int\n    init(_ n: Int) { self.n = n; tree = Array(repeating: 0, count: n+1) }\n    func update(_ i: Int) { var x=i; while x<=n { tree[x]+=1; x+=x & (-x) } }\n    func query(_ i: Int) -> Int { var s=0,x=i; while x>0 { s+=tree[x]; x-=x & (-x) }; return s }\n}\nfunc countInversions(_ arr: [Int]) -> Int {\n    let n=arr.count, sorted=arr.sorted(), bit=BIT(n)\n    var rank=[Int:Int](); for (i,v) in sorted.enumerated() { rank[v]=i+1 }\n    return arr.reduce(0) { acc, x in\n        let r=rank[x]!; bit.update(r); return acc + (bit.query(n) - bit.query(r))\n    }\n}",
      haskell: "-- Merge sort based O(n log n) inversion count\ncountInversions :: [Int] -> (Int, [Int])\ncountInversions []  = (0, [])\ncountInversions [x] = (0, [x])\ncountInversions xs  = (lc + rc + mc, merged)\n  where\n    mid = length xs \\`div\\` 2\n    (lc, ls) = countInversions (take mid xs)\n    (rc, rs) = countInversions (drop mid xs)\n    (mc, merged) = go ls rs 0\n    go [] ys c = (c, ys)\n    go xs [] c = (c, xs)\n    go (x:xs) (y:ys) c\n      | x <= y    = let (c',m) = go xs (y:ys) c in (c', x:m)\n      | otherwise = let (c',m) = go (x:xs) ys (c + length (x:xs)) in (c', y:m)"}},
    {id:12,title:'Sort Transformed Array',difficulty:'Medium',tags:['Two Pointers','Math'],
    description:'Given sorted array nums and coefficients a, b, c, apply f(x)=ax²+bx+c to each element and return in sorted order.',
    examples:[{input:'nums=[-4,-2,2,4],a=1,b=3,c=5',output:'[3,9,15,33]'}],
    constraints:['nums sorted in ascending order'],hint:'Parabola opens up (a>0): fill from end using two pointers. Opens down: fill from start.',
    timeComplexity:'O(n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun sortTransformedArray(nums: IntArray, a: Int, b: Int, c: Int): IntArray {\n    val n = nums.size\n    val res = IntArray(n)\n    fun f(x: Int) = a*x*x + b*x + c\n    var lo=0; var hi=n-1; var idx = if(a>=0) n-1 else 0\n    while (lo<=hi) {\n        val fl=f(nums[lo]); val fh=f(nums[hi])\n        if (a>=0) { if(fl>fh){res[idx--]=fl;lo++}else{res[idx--]=fh;hi--} }\n        else      { if(fl<fh){res[idx++]=fl;lo++}else{res[idx++]=fh;hi--} }\n    }\n    return res\n}",
      dart: "List<int> sortTransformedArray(List<int> nums, int a, int b, int c) {\n  int n=nums.length; List<int> res=List.filled(n,0);\n  int f(int x)=>a*x*x+b*x+c;\n  int lo=0, hi=n-1, idx=a>=0?n-1:0;\n  while(lo<=hi) {\n    int fl=f(nums[lo]),fh=f(nums[hi]);\n    if(a>=0){if(fl>fh){res[idx--]=fl;lo++;}else{res[idx--]=fh;hi--;}}\n    else    {if(fl<fh){res[idx++]=fl;lo++;}else{res[idx++]=fh;hi--;}}\n  }\n  return res;\n}",
      swift: "func sortTransformedArray(_ nums:[Int], _ a:Int, _ b:Int, _ c:Int) -> [Int] {\n    let n=nums.count; var res=Array(repeating:0,count:n)\n    func f(_ x:Int) -> Int { a*x*x+b*x+c }\n    var (lo,hi,idx) = (0,n-1,a>=0 ? n-1 : 0)\n    while lo<=hi {\n        let (fl,fh)=(f(nums[lo]),f(nums[hi]))\n        if a>=0 { if fl>fh {res[idx]=fl;lo+=1} else {res[idx]=fh;hi-=1}; idx-=1 }\n        else    { if fl<fh {res[idx]=fl;lo+=1} else {res[idx]=fh;hi-=1}; idx+=1 }\n    }\n    return res\n}",
      haskell: "sortTransformedArray :: [Int] -> Int -> Int -> Int -> [Int]\nsortTransformedArray nums a b c = Data.List.sort (map f nums)\n  where f x = a*x*x + b*x + c"}},
    {id:13,title:'Minimum Cost to Connect Ropes',difficulty:'Medium',tags:['Greedy','Heap'],
    description:'Given ropes of various lengths, connect them into one. Cost = sum of lengths of two ropes connected. Minimize total cost.',
    examples:[{input:'[4,3,2,6]',output:'29',explanation:'Connect 2+3=5(cost5), 4+5=9(cost9), 6+9=15(cost15). Total=29'}],
    constraints:['1 ≤ n ≤ 10⁴', '1 ≤ rope[i] ≤ 10⁴'],hint:'Always connect the two smallest ropes (greedy). Use min-heap.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "import java.util.PriorityQueue\nfun connectRopes(ropes: IntArray): Long {\n    val pq = PriorityQueue<Long>()\n    for (r in ropes) pq.offer(r.toLong())\n    var cost = 0L\n    while (pq.size > 1) {\n        val combined = pq.poll() + pq.poll()\n        cost += combined; pq.offer(combined)\n    }\n    return cost\n}",
      dart: "int connectRopes(List<int> ropes) {\n  List<int> pq = List.from(ropes)..sort();\n  int cost=0;\n  while(pq.length>1){\n    int a=pq.removeAt(0),b=pq.removeAt(0),c=a+b;\n    cost+=c; int i=pq.indexWhere((x)=>x>=c); pq.insert(i<0?pq.length:i,c);\n  }\n  return cost;\n}",
      swift: "func connectRopes(_ ropes: [Int]) -> Int {\n    var pq = ropes.sorted(); var cost = 0\n    while pq.count > 1 {\n        let combined = pq.removeFirst() + pq.removeFirst(); cost += combined\n        let i = pq.firstIndex { $0 >= combined } ?? pq.count\n        pq.insert(combined, at: i)\n    }\n    return cost\n}",
      haskell: "connectRopes :: [Int] -> Int\nconnectRopes [_] = 0\nconnectRopes ropes = combined + connectRopes (insertSorted combined rest)\n  where\n    sorted = Data.List.sort ropes\n    combined = head sorted + sorted !! 1\n    rest = drop 2 sorted\n    insertSorted x [] = [x]\n    insertSorted x (y:ys) = if x<=y then x:y:ys else y:insertSorted x ys"}},
    {id:14,title:'Count of Smaller Numbers After Self',difficulty:'Hard',tags:['BIT','Merge Sort'],
    description:'For each element, count how many elements to its right are smaller than it.',
    examples:[{input:'[5,2,6,1]',output:'[2,1,1,0]'}],
    constraints:['1 ≤ n ≤ 10⁵'],hint:'Process right to left with BIT, or use modified merge sort.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun countSmaller(nums: IntArray): List<Int> {\n    val n = nums.size; val result = IntArray(n)\n    val sorted = nums.toSortedSet().toList()\n    val rank = sorted.mapIndexed { i, v -> v to i+1 }.toMap()\n    val bit = IntArray(n + 1)\n    fun update(i: Int) { var x=i; while(x<=n){bit[x]++;x+=x and -x} }\n    fun query(i: Int): Int { var s=0; var x=i; while(x>0){s+=bit[x];x-=x and -x}; return s }\n    for (i in n-1 downTo 0) {\n        result[i] = query(rank[nums[i]]!! - 1)\n        update(rank[nums[i]]!!)\n    }\n    return result.toList()\n}",
      dart: "List<int> countSmaller(List<int> nums) {\n  int n=nums.length; List<int> res=List.filled(n,0);\n  List<int> sorted=(Set.from(nums).toList()..sort());\n  Map<int,int> rank={for(int i=0;i<sorted.length;i++) sorted[i]:i+1};\n  List<int> bit=List.filled(sorted.length+1,0);\n  void upd(int i){for(;i<=sorted.length;i+=i&(-i))bit[i]++;}\n  int qry(int i){int s=0;for(;i>0;i-=i&(-i))s+=bit[i];return s;}\n  for(int i=n-1;i>=0;i--){res[i]=qry(rank[nums[i]]!-1);upd(rank[nums[i]]!);}\n  return res;\n}",
      swift: "func countSmaller(_ nums: [Int]) -> [Int] {\n    let n=nums.count; var res=Array(repeating:0,count:n)\n    let sorted=Array(Set(nums)).sorted(); var rank=[Int:Int]()\n    for (i,v) in sorted.enumerated() { rank[v]=i+1 }\n    var bit=Array(repeating:0,count:sorted.count+1)\n    func update(_ i:Int){var x=i; while x<=sorted.count{bit[x]+=1;x+=x & (-x)}}\n    func query(_ i:Int)->Int{var s=0,x=i; while x>0{s+=bit[x];x-=x & (-x)}; return s}\n    for i in stride(from:n-1,through:0,by:-1){\n        res[i]=query(rank[nums[i]]!-1); update(rank[nums[i]]!)\n    }\n    return res\n}",
      haskell: "import qualified Data.Map as Map\n\ncountSmaller :: [Int] -> [Int]\ncountSmaller nums = reverse \\$ snd \\$ foldr go (Map.empty, []) nums\n  where\n    go x (seen, res) =\n      let smaller = Map.size \\$ Map.filterWithKey (\\\\k _ -> k < x) seen\n          seen' = Map.insertWith (+) x 1 seen\n      in (seen', smaller : res)"}},
    {id:15,title:'Global and Local Inversions',difficulty:'Medium',tags:['Sorting','Analysis'],
    description:'Determine if the number of global inversions equals the number of local inversions in a permutation of [0..n-1]. Global: i<j, arr[i]>arr[j]. Local: arr[i]>arr[i+1].',
    examples:[{input:'[1,0,2]',output:'true',explanation:'1 global inversion, 1 local inversion'}],
    constraints:['Each value 0..n-1 appears exactly once'],hint:'Local ⊆ Global. They\'re equal iff no non-adjacent inversions exist. Check: max(arr[0..i]) > arr[i+2].',
    timeComplexity:'O(n)',spaceComplexity:'O(1)',
    solutions:{
      kotlin: "fun isIdealPermutation(nums: IntArray): Boolean {\n    var maxSoFar = Int.MIN_VALUE\n    for (i in 0 until nums.size - 2) {\n        maxSoFar = maxOf(maxSoFar, nums[i])\n        if (maxSoFar > nums[i + 2]) return false\n    }\n    return true\n}",
      dart: "bool isIdealPermutation(List<int> nums) {\n  int maxSoFar=nums[0];\n  for(int i=0;i<nums.length-2;i++){\n    maxSoFar=nums[i]>maxSoFar?nums[i]:maxSoFar;\n    if(maxSoFar>nums[i+2])return false;\n  }\n  return true;\n}",
      swift: "func isIdealPermutation(_ nums: [Int]) -> Bool {\n    var maxSoFar = Int.min\n    for i in 0..<nums.count-2 {\n        maxSoFar = max(maxSoFar, nums[i])\n        if maxSoFar > nums[i+2] { return false }\n    }\n    return true\n}",
      haskell: "isIdealPermutation :: [Int] -> Bool\nisIdealPermutation nums = all check (zip3 prefMaxes (drop 1 nums) (drop 2 nums))\n  where\n    prefMaxes = scanl1 max nums\n    check (maxPref, _, next2) = maxPref <= next2"}},
    {id:16,title:'Reorder Data in Log Files',difficulty:'Medium',tags:['Custom Sort'],
    description:'Sort log files: letter-logs before digit-logs. Letter-logs sorted lexicographically by content, then by identifier. Digit-logs maintain relative order.',
    examples:[{input:'["dig1 8 1 5 1","let1 art can","dig2 3 6","let2 own kit dig","let3 art zero"]',output:'["let1 art can","let3 art zero","let2 own kit dig","dig1 8 1 5 1","dig2 3 6"]'}],
    constraints:[],hint:'Stable sort: separate letter and digit logs. Sort letter logs with custom comparator.',
    timeComplexity:'O(m·n log n)',spaceComplexity:'O(m·n)',
    solutions:{
      kotlin: "fun reorderLogFiles(logs: Array<String>): Array<String> {\n    val letters = mutableListOf<String>(); val digits = mutableListOf<String>()\n    for (log in logs) {\n        if (log.split(\" \")[1][0].isDigit()) digits.add(log) else letters.add(log)\n    }\n    letters.sortWith(compareBy({ it.substringAfter(\" \") }, { it.substringBefore(\" \") }))\n    return (letters + digits).toTypedArray()\n}",
      dart: "List<String> reorderLogFiles(List<String> logs) {\n  List<String> letters=[], digits=[];\n  for (String log in logs) {\n    if (RegExp(r'[0-9]').hasMatch(log.split(' ')[1][0])) digits.add(log);\n    else letters.add(log);\n  }\n  letters.sort((a,b){\n    String ac=a.substring(a.indexOf(' ')+1), bc=b.substring(b.indexOf(' ')+1);\n    int c=ac.compareTo(bc); if(c!=0)return c;\n    return a.substring(0,a.indexOf(' ')).compareTo(b.substring(0,b.indexOf(' ')));\n  });\n  return [...letters,...digits];\n}",
      swift: "func reorderLogFiles(_ logs: [String]) -> [String] {\n    var letters = [String](), digits = [String]()\n    for log in logs {\n        let parts = log.split(separator: \" \")\n        if parts[1].first!.isNumber { digits.append(log) } else { letters.append(log) }\n    }\n    let sorted = letters.sorted {\n        let (a, b) = ($0.drop(while:{$0 != \" \"}).dropFirst(), $1.drop(while:{$0 != \" \"}).dropFirst())\n        return a == b ? $0 < $1 : a < b\n    }\n    return sorted + digits\n}",
      haskell: "import Data.List (sortBy, isPrefixOf)\nimport Data.Char (isDigit, isAlpha)\n\nreorderLogFiles :: [String] -> [String]\nreorderLogFiles logs = letters ++ digits\n  where\n    isDigitLog s = isDigit (words s !! 1 !! 0)\n    (digits, letters') = span isDigitLog (reverse logs)\n    letters = sortBy cmp (reverse letters')\n    cmp a b = let (ka, va) = break (==' ') a; (kb, vb) = break (==' ') b\n              in case compare va vb of { EQ -> compare ka kb; x -> x }"}},
    {id:17,title:'Frequency Sort String',difficulty:'Medium',tags:['Counting Sort','String'],
    description:'Sort characters in a string by frequency of occurrence, highest frequency first.',
    examples:[{input:'"tree"',output:'"eert" or "eetr"',explanation:'e appears twice'}],
    constraints:['1 ≤ s.length ≤ 5×10⁵'],hint:'Count character frequencies, sort by frequency descending, rebuild string.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun frequencySort(s: String): String {\n    val freq = s.groupingBy { it }.eachCount()\n    return s.toList().sortedByDescending { freq[it] }.joinToString(\"\")\n}",
      dart: "String frequencySort(String s) {\n  Map<String,int> freq={};\n  for(String c in s.split(''))freq[c]=(freq[c]??0)+1;\n  List<String> chars=s.split('')..sort((a,b)=>freq[b]!-freq[a]!);\n  return chars.join();\n}",
      swift: "func frequencySort(_ s: String) -> String {\n    var freq = [Character: Int](); s.forEach { freq[$0, default: 0] += 1 }\n    return String(s.sorted { freq[$0]! > freq[$1]! })\n}",
      haskell: "import qualified Data.Map.Strict as Map\nimport Data.List (sortBy, group, sort)\nimport Data.Ord (comparing, Down(..))\n\nfrequencySort :: String -> String\nfrequencySort s = concatMap (uncurry (flip replicate)) pairs\n  where\n    pairs = sortBy (comparing (Down . snd))\n            [(head g, length g) | g <- group (sort s)]"}},
    {id:18,title:'Meeting Rooms II',difficulty:'Medium',tags:['Sorting','Heap','Greedy'],
    description:'Find the minimum number of conference rooms required to schedule all meetings.',
    examples:[{input:'[[0,30],[5,10],[15,20]]',output:'2'}],
    constraints:['0 ≤ intervals[i][0] < intervals[i][1] ≤ 10⁶'],hint:'Sort by start time. Use min-heap of end times. If earliest-ending room is free, reuse it; else add new room.',
    timeComplexity:'O(n log n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "import java.util.PriorityQueue\nfun minMeetingRooms(intervals: Array<IntArray>): Int {\n    intervals.sortBy { it[0] }\n    val rooms = PriorityQueue<Int>()  // end times\n    for (iv in intervals) {\n        if (rooms.isNotEmpty() && rooms.peek() <= iv[0]) rooms.poll()\n        rooms.offer(iv[1])\n    }\n    return rooms.size\n}",
      dart: "int minMeetingRooms(List<List<int>> intervals) {\n  intervals.sort((a,b)=>a[0]-b[0]);\n  List<int> ends=[]; // min-heap simulation\n  for(var iv in intervals) {\n    if(ends.isNotEmpty && ends[0]<=iv[0]) ends.removeAt(0);\n    ends.add(iv[1]); ends.sort();\n  }\n  return ends.length;\n}",
      swift: "func minMeetingRooms(_ intervals: [[Int]]) -> Int {\n    let sorted = intervals.sorted { $0[0] < $1[0] }\n    var ends = [Int]()  // min-heap via sorted array\n    for iv in sorted {\n        if let i = ends.firstIndex(where: { $0 <= iv[0] }) { ends.remove(at: i) }\n        let j = ends.firstIndex { $0 >= iv[1] } ?? ends.count\n        ends.insert(iv[1], at: j)\n    }\n    return ends.count\n}",
      haskell: "import Data.List (sort)\n\nminMeetingRooms :: [(Int,Int)] -> Int\nminMeetingRooms intervals = go (sort intervals) []\n  where\n    go [] rooms = length rooms\n    go ((s,e):rest) rooms =\n      let freeRoom = filter (<=s) rooms\n          rooms' = if null freeRoom then e:rooms\n                   else e : filter (>s) rooms\n      in go rest rooms'"}},
    {id:19,title:'Largest Rectangle in Histogram',difficulty:'Hard',tags:['Stack','Sorting'],
    description:'Find the area of the largest rectangle that can be formed in a histogram.',
    examples:[{input:'[2,1,5,6,2,3]',output:'10',explanation:'Rectangle of height 5, width 2'}],
    constraints:['1 ≤ n ≤ 10⁵', '0 ≤ heights[i] ≤ 10⁴'],hint:'Use a monotone increasing stack. Pop when we find a smaller bar; compute area using stack.',
    timeComplexity:'O(n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun largestRectangleArea(heights: IntArray): Int {\n    val stack = ArrayDeque<Int>(); var maxArea = 0\n    val h = heights + intArrayOf(0)\n    for (i in h.indices) {\n        while (stack.isNotEmpty() && h[stack.last()] > h[i]) {\n            val top = stack.removeLast()\n            val width = if (stack.isEmpty()) i else i - stack.last() - 1\n            maxArea = maxOf(maxArea, h[top] * width)\n        }\n        stack.addLast(i)\n    }\n    return maxArea\n}",
      dart: "int largestRectangleArea(List<int> heights) {\n  List<int> stack=[]; int maxArea=0;\n  List<int> h=[...heights,0];\n  for(int i=0;i<h.length;i++){\n    while(stack.isNotEmpty&&h[stack.last]>h[i]){\n      int top=stack.removeLast();\n      int w=stack.isEmpty?i:i-stack.last-1;\n      maxArea=h[top]*w>maxArea?h[top]*w:maxArea;\n    }\n    stack.add(i);\n  }\n  return maxArea;\n}",
      swift: "func largestRectangleArea(_ heights: [Int]) -> Int {\n    var stack = [Int](), maxArea = 0, h = heights + [0]\n    for i in h.indices {\n        while !stack.isEmpty && h[stack.last!] > h[i] {\n            let top = stack.removeLast()\n            let width = stack.isEmpty ? i : i - stack.last! - 1\n            maxArea = max(maxArea, h[top] * width)\n        }\n        stack.append(i)\n    }\n    return maxArea\n}",
      haskell: "largestRectangleArea :: [Int] -> Int\nlargestRectangleArea heights = go (heights ++ [0]) [] 0\n  where\n    go [] _ maxA = maxA\n    go (h:hs) stack maxA\n      | null stack || h >= heights' (head stack) =\n          go hs (length heights - length (h:hs) : stack) maxA\n      | otherwise =\n          let i = head stack\n              w = case tail stack of { [] -> length heights - length hs; (j:_) -> i - j - 1 }\n              a = heights' i * w\n          in go (h:hs) (tail stack) (max maxA a)\n    heights' i = (heights ++ [0]) !! i"}},
    {id:20,title:'Maximum Width Ramp',difficulty:'Medium',tags:['Sorting','Two Pointers'],
    description:'Find the maximum width ramp: a ramp is a pair (i,j) where i < j and A[i] ≤ A[j]. The width is j-i.',
    examples:[{input:'[6,0,8,2,1,5]',output:'4',explanation:'Ramp from index 1 (val 0) to index 5 (val 5), width=4'}],
    constraints:['2 ≤ n ≤ 5×10⁴'],hint:'Build decreasing stack for left candidates. Scan from right for right candidates.',
    timeComplexity:'O(n)',spaceComplexity:'O(n)',
    solutions:{
      kotlin: "fun maxWidthRamp(nums: IntArray): Int {\n    val n = nums.size; val stack = mutableListOf<Int>()\n    for (i in 0 until n) if (stack.isEmpty() || nums[stack.last()] > nums[i]) stack.add(i)\n    var maxW = 0; var j = n - 1\n    while (j >= 0 && stack.isNotEmpty()) {\n        while (stack.isNotEmpty() && nums[stack.last()] <= nums[j]) {\n            maxW = maxOf(maxW, j - stack.removeLast());\n        }\n        j--\n    }\n    return maxW\n}",
      dart: "int maxWidthRamp(List<int> nums) {\n  int n=nums.length; List<int> stack=[];\n  for(int i=0;i<n;i++) if(stack.isEmpty||nums[stack.last]>nums[i])stack.add(i);\n  int maxW=0,j=n-1;\n  while(j>=0&&stack.isNotEmpty) {\n    while(stack.isNotEmpty&&nums[stack.last]<=nums[j]){maxW=j-stack.removeLast()>maxW?j-stack.length:maxW;} // simplified\n    j--;\n  }\n  return maxW;\n}",
      swift: "func maxWidthRamp(_ nums: [Int]) -> Int {\n    var stack = [Int](), maxW = 0\n    for i in nums.indices { if stack.isEmpty || nums[stack.last!] > nums[i] { stack.append(i) } }\n    var j = nums.count - 1\n    while j >= 0 && !stack.isEmpty {\n        while !stack.isEmpty && nums[stack.last!] <= nums[j] {\n            maxW = max(maxW, j - stack.removeLast())\n        }\n        j -= 1\n    }\n    return maxW\n}",
      haskell: "maxWidthRamp :: [Int] -> Int\nmaxWidthRamp nums = maximum \\$ 0 : [j-i | i<-[0..n-1], j<-[i..n-1], nums!!i <= nums!!j]\n  where n = length nums\n\n-- O(n) version:\nmaxWidthRampLinear :: [Int] -> Int\nmaxWidthRampLinear nums =\n  let n = length nums\n      stack = foldl (\\\\s i -> if null s || nums!!(last s) > nums!!i then s++[i] else s) [] [0..n-1]\n  in foldl (\\\\best j -> foldl (\\\\b i -> max b (j-i)) best (takeWhile (\\\\i -> nums!!i <= nums!!j) (reverse stack))) 0 (reverse [0..n-1])"}}
  ]
}
